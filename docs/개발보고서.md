# 워터링 Android 개발보고서

> 마지막 업데이트: 2026-07-05 (v0.30.4)

---

## v0.30.4 — 체중 기반 목표 추천 적용 후 피드백 개선

- **배경**: 사용자가 "체중 기반 목표 추천받기"에서 몸무게를 입력하고 적용해도 다이얼로그가 조용히
  닫히고 끝이라 무슨 일이 일어났는지 알 수 없었음("체중 입력하면 그 다음 프로세스는 어떻게
  되는거야? 아무런 추천 멘트가 안뜨는데?"). AskUserQuestion으로 개선 범위를 확인해 두 가지를 적용:
  1. 적용 후 확인 메시지(스낵바)
  2. 설정 행 설명 문구가 마지막 입력 이력을 반영
  (다이얼로그에 계산 근거 한 줄을 추가하는 안은 이번엔 범위 밖으로 제외)
- **구현**:
  - `SettingsViewModel`에 `snackbarMessage: StateFlow<String?>`/`clearSnackbar()` 추가,
    `applyWeightGoal()`이 저장 직후 "OOkg 기준 하루 목표를 N잔으로 설정했어요" 메시지를 설정
  - `SettingsViewModel`에 `weightGoalSubtitle: StateFlow<String>` 추가 — 저장된 몸무게가 없으면
    기존 기본 안내문, 있으면 `StatsInsightService.recommendedGoalCups`로 재계산한 "마지막 입력:
    OOkg → 하루 N잔"을 보여줌(계산은 ViewModel에서, Composable은 문자열만 받음 — 아키텍처 규칙 준수)
  - `SettingsScreen`에 `SnackbarHost` 추가(이 화면 최초 도입), `WeightGoalRow`가 고정 문구 대신
    `subtitle: String` 파라미터를 받도록 변경
  - 문자열 `settings_weight_goal_applied_snackbar`/`settings_weight_goal_last_input` 신규 추가(ko/en)
  - `SettingsViewModelTest`에 스낵바 메시지 설정 검증, 부제 두 가지 분기(있음/없음) 검증 테스트 추가
- **실기기 검증**: Galaxy A25에서 65kg 입력 후 적용 → 스낵바 "65kg 기준 하루 목표를 11잔으로
  설정했어요" 노출 확인, "하루 목표"가 11잔으로 갱신 확인, 행 부제가 "마지막 입력: 65kg → 하루
  11잔"으로 바뀐 것 확인

## v0.30.3 — 안내 배너 레이아웃 개선 + 통계/설정 화면 그라데이션 배경 + 연속 기록 아이콘 통일

- **배경**: 세 가지 요청을 한 번에 처리
  1. "물 마시기 자동 저장" 아래 Health Connect 안내 배너가 아이콘+텍스트+버튼이 한 줄에 억지로 끼어
     버튼 글자가 줄바꿈되는 문제 — 웹 목업으로 3가지 대안(세로 스택형/하단 전체 너비 버튼형/카드
     전체 탭 가능형) 제시 후 ③ 카드 전체 탭 가능형으로 확정
  2. 백업 및 복원의 로그아웃 상태 안내도 동일한 형식으로 통일
  3. 통계·설정 화면 배경을 홈 화면과 같은 그라데이션으로, 통계 화면 연속 기록 카드의 불꽃·트로피
     아이콘도 홈과 동일한 벡터 아이콘으로 통일
- **구현**:
  - `HealthConnectVisibilityHint`(SettingsScreen.kt): `TextButton` 제거, `Row` 전체를 `clickable`로
    바꾸고 우측에 화살표(`Icons.AutoMirrored.Filled.KeyboardArrowRight`) 아이콘 추가. 본문 문구를
    "탭하면 Health Connect 설정으로 이동해요"로 축약, 이제 안 쓰는
    `settings_hydration_sync_other_app_open` 문자열 삭제
  - `BackupSection`의 로그아웃 상태 분기: 별도 "구글로 로그인" `TextButton` 제거, `Row` 전체
    `clickable(onClick = onSignIn)` + 우측 화살표로 변경(로그인된 상태의 "로그아웃" 버튼과 백업/복원
    버튼은 그대로 유지 — 실제 선택이 필요한 액션이라 범위 밖). 이제 안 쓰는 `settings_backup_sign_in`
    문자열 삭제
  - `ui/theme/Theme.kt`에 화면 공통 배경 그라데이션 `AppBackgroundGradient`(스카이 블루 #D3ECF3 →
    라벤더 퍼플 #E1D7F0) 상수 추가, `HomeScreen.kt`가 로컬로 갖고 있던 동일한 그라데이션 정의를 이걸
    참조하도록 리팩터(중복 제거), `StatsScreen.kt`/`SettingsScreen.kt`에도 동일한 `Box` + 투명
    `Scaffold`/`TopAppBar` 패턴 적용
  - `StatsScreen.kt`의 `StreakStatCard`를 `emoji: String` 대신 `icon: ImageVector, tint: Color`를
    받도록 변경, 홈 화면과 동일하게 `Icons.Filled.LocalFireDepartment`(tertiary)/
    `Icons.Filled.EmojiEvents`(secondary) 적용
- **실기기 검증**: Galaxy A25에서 Health Connect·백업 배너가 버튼 없이 카드 전체 탭 가능 + 화살표
  형태로 나오는 것 확인, 통계·설정 화면 배경이 홈과 동일한 그라데이션으로 나오는 것 확인, 통계 화면
  연속 기록 카드 아이콘이 홈과 동일한 벡터 아이콘(오렌지 불꽃/골드 트로피)으로 나오는 것 확인

## v0.30.2 — 설정 화면 섹션 순서 변경 (기록 설정 최상단으로)

- **배경**: Settings 화면에서 "기록 설정"(하루 목표/컵 크기/체중 목표/CSV 내보내기) 섹션을 가장 위로
  올려달라는 요청 — 기존엔 위젯 테마 섹션이 최상단이었음
- **구현**: `SettingsScreen.kt`의 `LazyColumn` 아이템 순서를 "기록 설정" 블록이 "위젯 테마" 블록보다
  먼저 오도록 재배치(내용 자체는 변경 없이 순서만 이동)
- **실기기 검증**: Galaxy A25에서 설정 화면 진입 시 "기록 설정"(하루 목표/컵 크기/체중 기반 목표/CSV
  내보내기)이 최상단에, 그다음 "위젯 테마", "건강 연동" 순으로 나오는 것 확인

## v0.30.1 — 연속 기록 카드/기록 리스트 배경색을 크림베이지 계열로 변경

- **배경**: v0.29.10에서 아쿠아 틴트로 바꿨던 카드 배경을 파란 계열 말고 다른 파스텔로 다시 바꿔달라는
  요청 — 웹 목업으로 논-블루 파스텔 10종(라벤더/로즈핑크/피치/민트그린/버터옐로우/라일락/코럴/
  세이지그린/살몬/크림베이지) 비교 → 버터옐로우 → 세이지그린 → 크림베이지 순으로 실기기에서 순차
  비교 → 크림베이지가 너무 진하다는 피드백으로 더 연한 버전 3단계(#FBF7EF/#FDFAF4/#FEFCF8) 추가 비교
  → 최종 `#FDFAF4`로 확정(카드 경계가 배경과 적당히 구분되면서 가장 자연스러움)
- **구현**: `HomeScreen.kt`의 `CardBackgroundColor`를 `#EAF7FB`(아쿠아 틴트)에서 `#FDFAF4`(연한 크림)로 변경
- **실기기 검증**: Galaxy A25에서 여러 색상 후보를 순차 설치하며 비교, 최종 색상으로 연속 기록 카드·
  기록 리스트가 배경 그라데이션과 잘 어울리는 것 확인

## v0.30.0 — 프리미엄 구독 제거, 전 기능 무료 전환

- **배경**: 사용자 결정으로 프리미엄 구독 자체를 없애고 모든 기능을 무료로 전환. 결제/구독 관련 코드
  (BillingService/PremiumScreen/PremiumViewModel)는 나중에 되살릴 수 있도록 파일은 남기되, Settings/
  Stats/Home에서 그 화면으로 가는 진입점(배너, 잠금 아이콘, 네비게이션 콜백)만 전부 제거. `UserSettings.isPremium`
  필드는 완전히 삭제(AskUserQuestion으로 확인: 코드는 남김+진입점만 제거 / isPremium 필드는 완전 제거,
  두 옵션 모두 "권장" 선택)
- **구현 (핵심 변경만 요약, 실제로는 12개 파일 + 문자열 리소스 전반에 걸친 대규모 리팩터)**:
  - `UserSettings.kt`: `isPremium` 필드 삭제
  - `SettingsDataStore.kt`/`SettingsRepository.kt`: 이제 쓸모없어진 `updatePremium()` 삭제
  - `AchievementChecker.kt`: `isPremium` 파라미터 삭제, 60/100/365일 연속 기록 타이틀을 프리미엄 게이팅 없이 항상 도달 가능하도록 변경. `Achievement.kt`의 미사용 `isPremiumOnly` 필드도 함께 정리
  - `WaterRepository.kt`/`WaterService.kt`: 연속 기록 보호권(한 달에 하루 놓쳐도 유지)을 `isPremium` 조건 없이 모든 유저에게 항상 적용
  - `WateringWidgetState.kt`/`AddWaterAction.kt`/`HomeViewModel.kt`: 위 시그니처 변경에 맞춰 호출부 정리, 위젯 테마도 이제 프리미엄 체크 없이 항상 선택한 테마 그대로 렌더링
  - `SettingsScreen.kt`: "프리미엄으로 업그레이드" 배너 삭제, 위젯 테마 스와치의 자물쇠·흐림 효과 삭제(항상 선택 가능), CSV 내보내기 행의 자물읤 삭제(항상 가능), `[DEBUG] 프리미엄 켜기/끄기` 토글 삭제(더 이상 개념 자체가 없음)
  - `StatsScreen.kt`: "스마트 통계" CTA 카드의 자물쇠 삭제, 항상 `SmartStatsScreen`으로 바로 진입
  - `WateringNavGraph.kt`: Home/Stats/Settings에서 Premium 화면으로 가는 `onNavigateToPremium` 콜백 전부 제거(라우트 등록 자체는 남겨둠 — PremiumScreen.kt는 코드상 존재하되 진입할 방법이 없는 휴면 상태)
  - `BillingService.kt`: 더 이상 존재하지 않는 `isPremium` 필드에 쓰던 `settingsRepository.updatePremium()` 호출 제거, 자체 `_isPremium` StateFlow(구매 상태 추적용)는 그대로 유지
  - `StatsViewModel.kt`: `StatsUiState.isPremium` 필드 삭제
  - 문자열: 프리미엄 잠금 전용 문자열(`settings_premium_active/upgrade`, `settings_csv_export_locked_hint`, `settings_widget_theme_locked_hint`) 삭제, `PremiumScreen`/`HydrationSyncDialogs`가 공유하던 일반 문자열 2개는 `settings_health_connect_*`로 이름 정리해 유지
  - 테스트: `SettingsRepositoryTest`(updatePremium 테스트 삭제), `WaterRepositoryTest`(9건 시그니처 정리, "비프리미엄" 테스트 삭제·이름 정리), `AchievementCheckerTest`(6건 시그니처 정리, "프리미엄 아니면 STREAK_30 제한" 테스트 3건 삭제 — 이제 그런 제한이 없음), `WaterServiceTest`/`HomeViewModelTest` 시그니처 정리. 전체 테스트 통과
- **실기기 검증**: Galaxy A25에서 Settings 화면(배너 없음, 위젯 테마 8종 전부 잠금 없이 선명하게 표시,
  CSV 내보내기 자물쇠 없음), Stats 화면("스마트 통계" 카드 자물쇠 없이 탭하면 바로 `SmartStatsScreen`
  진입) 모두 확인

## v0.29.15 — "물 마셨어요" 버튼에 살짝 그림자 추가

- **배경**: 메인 버튼("물 마셨어요")도 살짝 그림자를 줬으면 좋겠다는 요청 — 웹 목업으로 그림자 강도
  3단계(살짝/중간/확실히) 비교 후 ① 살짝(2dp급)으로 확정
- **구현**: `HomeScreen.kt`의 메인 `Button`에 `elevation = ButtonDefaults.buttonElevation(defaultElevation = 2.dp)` 추가
- **실기기 검증**: Galaxy A25에서 은은한 그림자 확인

## v0.29.14 — "다른 음료 선택" 버튼 디자인 변경(화이트 카드)

- **배경**: "다른 음료 선택" 버튼 디자인을 바꿔달라는 요청 — 웹 HTML 목업으로 6가지 후보(현재
  스타일/아쿠아 채우기/라벤더 아웃라인/라벤더 채우기/화이트 카드/텍스트만)를 보여준 뒤 화이트
  카드 스타일로 확정
- **구현**: `HomeScreen.kt` — 아쿠아 테두리 `OutlinedButton`을 `Button`으로 교체, 흰색 배경
  (`containerColor = Color.White`) + 아쿠아 텍스트(`contentColor = MaterialTheme.colorScheme.primary`)
  + 그림자(`elevation = 3.dp`), 테두리 없음. 더 이상 쓰이지 않는 `OutlinedButton` import 제거
- **실기기 검증**: Galaxy A25에서 테두리 없는 흰 배경 + 그림자 + 아쿠아 텍스트로 바뀐 것 확인

## v0.29.13 — 2×2 위젯 텍스트 크기 확대

- **배경**: 2×2(Rectangular) 위젯의 "물방울 아이콘 + 워터링" 헤더와 하단 "목표 달성!" 문구가 너무
  작아 보인다는 요청
- **구현**: `RectangularWidget.kt` — 물방울 아이콘 13.dp→16.dp, "워터링" 라벨 11.sp→13.sp, 하단
  동기부여 문구(`motivationText`, 달성 시 "목표 달성! 🎉" 포함) 11.sp→13.sp
- **실기기 검증**: Galaxy A25 홈 화면 2×2 위젯에서 두 텍스트 모두 커진 것 확인

- **배경**: 홈 화면 "목표 달성! 🎉" 문구가 너무 작아 보인다는 요청
- **구현**: `HomeScreen.kt`의 `AchievementRing` 내 달성 문구 `Text`를 `MaterialTheme.typography.labelMedium`(12sp)
  대신 `fontSize = 20.sp`로 직접 지정 — 문구와 이모지가 같은 문자열이라 크기 변경 하나로 둘 다 커짐
- **실기기 검증**: Galaxy A25에서 "목표 달성! 🎉"가 눈에 띄게 커진 것 확인

## v0.29.11 — 목표 달성 색상 골드 → 라벤더(소프트 퍼플)로 변경

- **배경**: 홈 화면 배경/카드 색을 파스텔로 바꾼 김에, 목표 달성 시 색상(골드 `#F5C860`)도 라벤더
  계열로 바꿔달라는 요청 — 웹 HTML 목업으로 6가지 라벤더 파스텔 후보를 보여준 뒤 소프트 퍼플로 확정
- **구현**: `HomeScreen.kt`에 `AchievedColor = Color(0xFFBFA8DE)` 상수 추가, `AchievementRing`의
  달성 시 링 색상과 "목표 달성!" 텍스트 색상을 `MaterialTheme.colorScheme.secondary`(골드) 대신 이
  색으로 교체. **범위는 달성 링/텍스트로만 한정** — "최장 연속" 트로피 아이콘 tint는 요청 범위 밖이라
  기존 골드(`secondary`) 그대로 유지(트로피는 시상 상징색이라 의도적으로 보존)
- **실기기 검증**: Galaxy A25에서 7/7잔 달성 상태로 링·텍스트가 소프트 퍼플로 바뀌고, 트로피는 골드
  그대로인 것 확인

## v0.29.10 — 연속 기록 카드/기록 리스트 배경색 변경

- **배경**: v0.29.9에서 홈 화면에 배경 그라데이션을 넣고 나니, 연속 기록 카드(현재 연속/최장 연속)와
  오늘 기록 리스트가 기존 회색(`MaterialTheme.colorScheme.surfaceVariant`)이라 눈에 띈다는 요청 —
  웹 HTML 목업으로 6가지 후보(화이트/아쿠아 틴트/라벤더 틴트/민트 틴트/피치 틴트/버터옐로우 틴트)를
  그라데이션 배경 위에 올려 비교한 뒤 ② 아쿠아 틴트로 확정
- **구현**: `HomeScreen.kt`에 `CardBackgroundColor = Color(0xFFEAF7FB)` 상수 추가, `StreakCard`/
  `WaterEntryRow`의 `Surface(color = MaterialTheme.colorScheme.surfaceVariant)`를 이 고정 색으로 교체
- **실기기 검증**: Galaxy A25에서 연속 기록 카드·기록 리스트가 아쿠아 틴트로 바뀌고 배경 그라데이션과
  자연스럽게 어울리는 것 확인

## v0.29.9 — 체중 기반 목표 추천 무료 전환(직접 입력) + 홈 화면 배경 그라데이션

- **배경**: 사용자 결정으로 "체중 기반 목표 추천받기"를 프리미엄에서 제외하고 무료로 전환, Health
  Connect 자동 읽기 대신 사용자가 몸무게를 직접 입력하는 방식으로 변경(AskUserQuestion으로 확인:
  Health Connect 완전 제거 + 입력값 저장 방식 모두 "권장" 선택). 홈 화면 배경색은 웹 HTML 목업으로
  여러 차례(플랫 파스텔 → 대비 강화 플랫 → 그라데이션 → 연한 그라데이션) 후보를 보여준 뒤 최종
  확정
- **구현 — 체중 기반 목표**:
  - `UserSettings.weightKg: Double?` 신규 필드로 입력한 몸무게를 저장, 재사용
  - `HealthConnectService`에서 `WEIGHT_PERMISSIONS`/`readLatestWeightKg()` 제거(더 이상 불필요,
    수분 자동 저장 로직은 그대로 유지)
  - `SettingsViewModel.WeightGoalUiState`를 7종 상태(Idle/Loading/NeedsPermission/PermissionDenied/
    NotAvailable/NoWeightData/Recommended)에서 `Idle`/`Editing(weightInput, recommendedCups)` 2종으로
    단순화. `openWeightGoalDialog()`(저장된 몸무게 있으면 미리 채움)/`onWeightInputChange()`(입력마다
    실시간 추천 잔수 계산)/`applyWeightGoal()`(몸무게+목표 동시 저장) 신규
  - `SettingsScreen.kt`: `WeightGoalRow`에서 프리미엄 잠금 제거(항상 무료), `WeightGoalDialogs`를
    `OutlinedTextField`(kg 입력) + 실시간 "추천 목표: 하루 N잔" 표시 + 적용/취소 단일 다이얼로그로
    전면 재작성. 하이드레이션 동기화 다이얼로그와 공유하던 문자열 2개(`settings_weight_goal_open_play_store`/
    `settings_weight_goal_dialog_ok`)는 `settings_health_connect_*`로 이름 정리
  - `PremiumScreen.kt`의 `PREMIUM_FEATURES`에서 "스마트 목표 추천"(🎯) 항목 제거, 관련 문자열 정리
  - 테스트: `SettingsViewModelTest` 체중 관련 7건 → 6건으로 재작성(신규 플로우 검증), `HealthConnectServiceTest`에서
    `WEIGHT_PERMISSIONS` 테스트 제거. 전체 170개 통과
  - **실기기 검증**: Galaxy A25에서 자물쇠 없이 무료로 노출 확인 → 65kg 입력 시 "추천 목표: 하루
    11잔" 실시간 계산 확인 → 적용 시 하루 목표 11잔으로 저장 확인 → 다이얼로그 재진입 시 저장된
    65kg과 추천값이 자동으로 불러와짐 확인 → 프리미엄 화면에서도 스마트 목표 항목이 빠진 것 확인
- **구현 — 홈 화면 배경 그라데이션**: `HomeScreen.kt`의 `Scaffold`를 `Box`로 감싸 `Brush.linearGradient`
  배경 적용, `Scaffold`/`TopAppBar` 컨테이너 색을 투명 처리해 그라데이션이 최상단까지 보이도록 함.
  최종 확정 색상은 스카이 블루(#D3ECF3) → 라벤더 퍼플(#E1D7F0) 연한 대각선 그라데이션(웹 목업 v4의
  ① 후보). 카드류(스트릭 카드, 기록 리스트)는 기존 `surfaceVariant` 그대로 유지(요청 범위 밖)
  - **실기기 검증**: Galaxy A25에서 홈 화면에 그라데이션이 상단부터 하단까지 자연스럽게 적용된 것 확인

## v0.29.8 — Health Connect 다른 앱 미노출 안내 배너 추가

- **배경**: v0.29.7에서 조사한 "삼성 헬스 등에서 물 기록이 안 보이는" 원인(삼성 헬스가 Health Connect
  읽기 권한 미허용)을 사용자가 직접 겪을 수 있으므로, 설정 화면에 안내를 추가해달라는 요청
- **구현**: `SettingsScreen.kt`에 `HealthConnectVisibilityHint` 배너 신규 — 기존 "알림 권한 배너"와
  동일한 스타일(아이콘 + 제목/부제 + 버튼)로 통일. "물 마시기 자동 저장" 토글이 켜져 있을 때만
  토글 바로 아래 노출. 버튼 탭 시 `android.health.connect.action.HEALTH_HOME_SETTINGS` 인텐트로
  시스템 Health Connect 홈 화면으로 이동(실기기에서 정상 동작 확인). 한국어/영문 문구 추가
- **실기기 검증**: Galaxy A25에서 토글 ON 상태로 배너 노출 확인(문구/스타일 스크린샷 확인 완료)

- **배경**: v0.28.2에서 "위젯으로 기록 시 업적이 인식되긴 하지만(Analytics/dedup 정상), 위젯에는
  모달을 띄울 화면이 없어 사용자가 실제로 못 본다"는 갭을 남은 작업으로 남겨뒀던 것을 해소. 사용자가
  "직접 검증하고 코딩할 수 있는 부분을 찾아보라"고 요청해 착수
- **구현**: `AchievementDataStore`에 `setPendingDisplay`/`consumePendingDisplay` 추가(기존
  `earned_today` dedup용 키와 별도의 `pending_display` 키, 앱↔위젯이 공유하는 동일 DataStore 재사용).
  위젯(`AddWaterAction`)은 업적 달성 시 이제 `achievementDataStore.setPendingDisplay(achievement)`를
  호출해 대기 상태로 저장하고, `HomeViewModel`의 `init` 블록에서 `consumePendingDisplay()`를 확인해
  있으면 `pendingAchievement`에 반영 → 앱 재진입 시 `AchievementDialog`가 자동으로 뜸. 앱 내
  `addWater()`/`addWaterCustom()` 경로는 기존과 동일하게 즉시 모달을 띄우며 이 대기 상태를 건드리지
  않음(위젯 경로에서만 필요)
- **테스트**: `HomeViewModelTest`에 2건 추가(대기 중인 업적을 init에서 로드, 없으면 null 유지) —
  전체 11개 통과
- **실기기 검증**: Galaxy A25에서 Health Connect 최근 액세스 로그로 위젯 경로의 실제 동작(총 기록
  수 증가, `achievements.preferences_pb`의 `earned_today` 갱신)까지는 확인했으나, 이번 세션 중 실기기
  터치 입력이 반복적으로 불안정해져(v0.29.5에서도 동일 현상 기록됨) `pending_display` 저장 → 앱
  재진입 시 모달 노출까지의 전체 왕복을 명확히 재현하지 못함 — 단위 테스트로 로직은 검증 완료, 실사용
  중 노출 여부는 다음 세션에서 재확인 필요
- **Health Connect 동기화 조사 (사용자 제보 "물 마신 게 헬스 앱에 연동 안 되는 것 같다")**: 코드
  버그 아님으로 결론. `UserSettings.healthConnectEnabled=true` 확인, Health Connect 시스템 설정의
  "최근 데이터에 액세스한 앱"에 워터링이 정상적으로 기록·시각까지 남아있어 `WaterService`의 쓰기
  동기화(v0.26)는 정상 동작 중임을 확인. 원인은 "앱 권한" 화면에서 삼성 헬스가 "액세스가 허용되지
  않은 앱"으로 분류돼 있는 것 — 워터링은 설계상 쓰기 전용 권한만 요청하고(로컬 DataStore가 진실의
  원천), 삼성 헬스가 Health Connect의 수분(Hydration) 데이터를 읽어오려면 삼성 헬스 앱 자체에서
  별도로 읽기 권한을 요청/허용해야 함 — 워터링 코드로 해결할 수 있는 범위 밖. 사용자에게 삼성 헬스
  앱에서 Health Connect 연동 설정을 확인하도록 안내 필요

## v0.29.6 — 앱 아이콘 어댑티브 세이프존 크기 수정

- **배경**: 남은 작업에 원인만 파악되고 미착수로 남아있던 "앱 아이콘 물방울이 확대돼 보이는 문제"를
  사용자 요청으로 실제 수정
- **원인 재확인**: `mipmap-anydpi-v26/ic_launcher.xml`/`ic_launcher_round.xml`이 참조하는
  `drawable/watering_icon3_fg.png`(1024×1024)의 불투명 콘텐츠(원형 배지+물방울)가 캔버스의 약 99.6%
  (bbox x/y `[0,1020]`)를 차지 — Android Adaptive Icon 세이프존(108dp 캔버스 중심 지름 66dp 원, 1024px
  기준 약 61%)보다 훨씬 크게 그려져 있어 런처 마스크 적용 시 바깥 테두리가 잘려 확대돼 보이는 것으로 확인.
  과거(미사용) 자동 생성 레거시 에셋은 정확히 66.7% 비율로 제작돼 있었던 것과 대조돼 원인이 명확함
- **수정**: 배경(`icon_transparent.xml`, 완전 투명)은 그대로 두고, `watering_icon3_fg.png` 안의 원형
  배지+물방울 디자인을 0.62배로 축소해 1024×1024 투명 캔버스 정중앙(사방 194px 여백, 콘텐츠 bbox
  `[194,828]`)에 재배치 — 파일명 유지로 두 XML 모두 자동 반영. Play 스토어 512 아이콘/레거시 mipmap
  아이콘(`watering_icon3_bg.png`)은 마스크 대상이 아니므로 범위 밖으로 유지, 변경 없음
- **검증**: 실기기(Galaxy A25, SM-A256N)에 `installDebug`로 설치 후 홈 화면 아이콘 스크린샷으로 확인 —
  물방울 배지가 마스크 안에 잘리지 않고 다른 시스템 아이콘과 비슷한 비율로 표시됨을 확인

## v0.28 — 기록 CSV 내보내기 (프리미엄 재기획 5순위)

- **배경**: 프리미엄 재기획 5순위이자 마지막 항목(기획서 8-2절). 전체 물 마시기 기록을 CSV 파일로
  내보내는 기능으로, 관련 코드가 전무했던 신규 영역(FileProvider·파일 공유 Intent를 이 앱에 처음
  도입). 4순위(연속 기록 마일스톤 확장)가 v0.27로 완료된 직후 착수
- **구현 내용**:
  - `CsvExportService` 신규(`core/service`) — `WaterRepository.getHistory()`로 전체 기록(최대 90일,
    오늘 포함)을 조회해 `WaterEntry` 단위 1행(날짜/시간/음료/양(ml))으로 CSV 생성. UTF-8 BOM을
    앞에 붙여 Windows Excel에서 한글 헤더가 깨지지 않도록 함. `context.cacheDir/csv/`에 파일 저장 후
    `FileProvider.getUriForFile()`로 `content://` URI 반환
  - `AndroidManifest.xml`에 `androidx.core.content.FileProvider` `<provider>` 신규 등록(`res/xml/file_paths.xml`의
    `<cache-path path="csv/">` 참조) — 앱 최초의 파일 공유 인프라
  - `SettingsViewModel`에 `CsvExportUiState`(Idle/Loading/Success/Error) 일회성 상태와 `exportCsv()`
    추가. `SettingsScreen`의 "기록 설정" 섹션에 `WeightGoalRow`와 동일한 프리미엄 잠금 패턴의
    `CsvExportRow` 추가 — 잠금 해제 시 `Intent.ACTION_SEND` + `FLAG_GRANT_READ_URI_PERMISSION`으로
    공유 시트 실행, 실패 시 Toast로 안내
  - `PremiumScreen.PREMIUM_FEATURES`에 "Data Export" 항목 추가(구현 완료로 미구현 목록 주석에서 제거)
  - 한국어/영문 `strings.xml` 양쪽에 신규 문자열 반영
  - 콤마/따옴표 이스케이프는 미구현 — `DrinkType` 6종 표시명에 특수문자가 없어 현재는 불필요(추후
    드러날 경우 추가 필요)
- **테스트**: `CsvExportServiceTest` 신규 5개(빈 히스토리 헤더만 생성, 여러 날짜/엔트리 시간순 정렬,
  UTF-8 BOM 포함 확인, 음료 타입 한글 매핑, 히스토리 조회 실패 시 `Result` 실패 반환) + 기존
  `SettingsViewModelTest`에 신규 의존성 주입 반영 — 전체 170개 통과
- **에뮬레이터 실측 검증 완료**: `UserSettings.isPremium` 기본값을 임시로 `true`로 바꿔 재빌드 후
  Pixel_8(Android 14)에서 실제 기록 추가 → CSV 내보내기 탭 → 공유 시트에 `watering_기록_2026-07-04.csv`
  파일명 정상 표시 확인. `run-as`로 캐시 파일을 직접 열어 UTF-8 BOM(`efbbbf`)과 헤더(`날짜,시간,음료,양(ml)`),
  날짜순 정렬된 행 데이터가 정확함을 확인. 비프리미엄(기본값 `false`) 상태에서는 자물쇠 아이콘 +
  "Unlock with Premium" 힌트가 뜨고, 탭 시 Premium 화면으로 정확히 이동하며 새 "Data Export" 항목이
  목록에 노출됨을 확인(`isPremium=true` 원상복구 후 최종 커밋)
- **다음 단계**: 프리미엄 재기획 5개 항목(위젯 테마/스마트 통계/스마트 목표+환산율/연속 기록
  마일스톤/CSV 내보내기) 전체 완료. 남은 후순위 항목(대기질·폭염 알림 등)은 우선순위 밖으로 유지

## v0.29.5 — 위젯 목표 달성 시 색상 반전(선택한 테마 색 배경 + 흰 글자)

- **배경**: 위젯 테마를 바꿔도 100% 달성 시엔 항상 고정 골드 배경(`ACHIEVED_COLOR`)이 나오는 게
  사용자 의도와 다름 — "달성 시 선택한 테마 색으로 배경이 바뀌고, 달성 전 글자색/배경색이 반전되어
  보였으면 좋겠다"는 요청
- **구현**: `WidgetTheme.ACHIEVED_COLOR`(고정 골드) 완전 제거. 3개 위젯(`CircularWidget`/
  `RectangularWidget`/`NarrowWidget`) 전부 `accent = state.theme.accentColor`로 통일하고, "달성
  시(`isAchieved`) 다크모드 여부와 무관하게 테마 색 배경 + 흰 텍스트로 반전" 규칙을 추가
  - `CircularWidget`: 기존에도 다크모드에서 배경 전체가 accent색이 되는 구조였어서, `invert =
    isDark || isAchieved`로 조건만 확장(다크모드 미달성 상태의 기존 모습은 변화 없음)
  - `RectangularWidget`/`NarrowWidget`: 원래 다크모드에서도 배경이 고정 남색(`DarkBg`)이고 텍스트만
    accent색이었던 구조라, "미달성 상태의 다크모드 스타일"은 그대로 두고 "달성 시"에만 배경을
    테마색으로, 전경(아이콘/텍스트/바)을 흰색으로 바꾸는 별도 분기(`foregroundAccent`)를 추가 —
    다크모드 자체의 기존 디자인은 건드리지 않음
- [x] `./gradlew testDebugUnitTest` 통과
- **참고**: 실기기에서 최종 시각 확인은 기기의 터치 인식이 일시적으로 불안정해져(adb 터치 주입은
  반응 없음, 방향키/위젯 탭은 정상) 완료하지 못함 — 사용자가 다음 세션에서 직접 확인 후 디자인
  세부사항(배경/글자색 조합 등) 추가 피드백 예정

- **위젯 테마 파스텔 8종 확장**: 기존 4종(기본/선셋/모노/네온)을 사용자가 웹 목업(HTML 12개 후보)에서
  고른 8종으로 교체 — 아쿠아/민트/라벤더/코랄/로즈핑크/스카이블루/버터옐로우/그레이모노.
  `WidgetTheme.kt`의 enum 값(이름은 하위 호환 위해 `DEFAULT` 유지, 표시명·색상만 교체)과 `strings.xml`
  (한/영) 갱신. `SettingsScreen.kt`의 `WidgetThemeSetting`을 `Row` → `FlowRow`로 변경해 8개 스와치가
  2줄로 자연스럽게 줄바꿈되도록 수정. 기존 테스트의 `WidgetTheme.SUNSET` 참조를 `MINT`로 교체
- **온보딩 다크모드 검정 글씨 버그 수정**: "다크 모드에서 앱 소개(온보딩) 글자가 검게 보인다" 제보.
  원인은 `OnboardingScreen.kt`가 `HomeScreen`/`SettingsScreen`과 달리 `Scaffold`나 `Surface` 없이
  최상위를 그냥 `Column`으로 시작한 것 — Compose에서 `Surface`가 없으면 색 미지정 `Text`가
  `LocalContentColor`의 기본값(검정)으로 렌더링되어 테마와 무관하게 항상 검게 나오는 것으로 확인.
  `Column`을 `Surface(color = MaterialTheme.colorScheme.background)`로 감싸 해결. 다른 Screen들은
  전부 `Scaffold`를 이미 쓰고 있어 동일 버그 없음을 확인
  - [x] `WidgetTheme.SUNSET` → `MINT` 테스트 참조 수정, `./gradlew testDebugUnitTest` 통과
  - [x] 실기기에서 앱 데이터 초기화 후 다크모드 온보딩 화면 재확인 — 전체 텍스트 흰색으로 정상 표시

- **배경**: 프리미엄 테스트 모드로 실기기 확인 중 사용자가 3가지 문제를 제보. 서브에이전트 3개로
  병렬 조사
- **① 위젯 테마를 눌러도 안 바뀜 — 코드 버그 아님, 조사만**: `CircularWidget.kt`/`RectangularWidget.kt`/
  `NarrowWidget.kt` 전부 `val accent = if (isAchieved) WidgetTheme.ACHIEVED_COLOR else
  state.theme.accentColor` 구조라, 목표를 이미 달성한 상태(테스트 기록이 세션 내내 7/5 달성 상태)에서는
  테마와 무관하게 항상 고정 골드가 나옴 — 저장/전파 경로(SettingsViewModel → SettingsRepository →
  SettingsDataStore → WateringWidgetUpdater.updateAll() → loadWidgetState())는 전부 정상 확인. 미달성
  상태에서 테스트하면 테마가 정상 반영됨
- **② 체중 데이터 없을 때 Health Connect 이동 — 구현 완료**: `SettingsScreen.kt`의
  `WeightGoalDialogs`(`NoWeightData` 분기)에 버튼 추가 — Health Connect 앱을 패키지명
  (`com.google.android.apps.healthdata`)으로 직접 실행, 설치 안 돼 있으면 기존 `NotAvailable` 분기와
  동일한 플레이스토어 인텐트로 폴백. Health Connect SDK(androidx.health.connect.client 1.1.0)엔
  "체중 입력 화면으로 바로 이동"하는 공식 딥링크가 없어(데이터 접근 전용 SDK) 앱 홈 화면 이동이
  현실적으로 가능한 최선
  - [x] `settings_weight_goal_open_health_connect` 문자열 추가(한/영)
  - [x] `./gradlew testDebugUnitTest` 통과, 실기기 설치 확인
- **③ 알림 30분 간격 미수신 — 코드 버그 아닐 가능성 높음, 조사만**: `NotificationService.kt`가
  30분을 `PeriodicWorkRequestBuilder(30, MINUTES, 15, MINUTES)`로 그대로 전달 — WorkManager 최소값
  (주기 15분/flex 5분) 만족하므로 스케줄링 자체는 정상. 하지만 `NotificationWorker.doWork()`에 조용히
  스킵하는 조건 3가지 존재: (a) 현재 시각이 알림 시작~종료 시간(기본 8~22시) 밖, **(b) 오늘 목표를
  이미 달성한 경우**, (c) `POST_NOTIFICATIONS` 권한 없음 — 셋 다 로그 없이 `Result.success()`로
  조용히 종료. 테스트 기기 기록이 세션 내내 달성 상태였으므로 (b) 때문에 스킵되고 있을 가능성이 높음
  (의도된 "목표 달성한 유저에겐 알림 안 보냄" 설계). 추가로 One UI 백그라운드 freeze가 WorkManager
  최초 실행 타이밍을 늦출 수 있음(배터리 최적화 예외 미설정 시)
  - **후속 과제로 고려**: 스킵 조건에 로그를 남기면 다음에 이런 문의가 왔을 때 더 빨리 진단 가능 —
    이번엔 범위 밖으로 미루고 사용자에게 체크리스트(달성 여부/시간대/권한/배터리 최적화)만 안내
- **범위 밖**: ①③은 코드 변경 없음 (원인이 코드 버그가 아니라 정상 동작/기기 설정으로 확인됨)

- **배경**: Play Console 상품이 아직 미등록이라 실제 결제로 프리미엄 기능을 테스트할 수 없는 상태에서,
  이전 세션들처럼 `UserSettings.isPremium` 기본값을 임시로 바꿔 재빌드하는 방식은 이미 로컬에 저장된
  설정값(예: 하루 목표 5잔 등)에는 영향을 주지 못하고, 대안으로 시도한 "설정 DataStore 파일 삭제"는
  하루 목표·컵 크기·알림 설정 등 다른 설정까지 전부 초기화시키는 과도한 방법이라 자동 승인 정책에
  의해 차단됨 — 더 안전한 방법으로 코드 기반 토글을 추가하기로 결정
- [x] `app/build.gradle.kts` — `buildFeatures.buildConfig = true` 추가(`BuildConfig` 클래스 생성 활성화,
  기존엔 `compose = true`만 있었음)
- [x] `SettingsViewModel.kt` — `toggleDebugPremium()` 추가, 기존 `isPremium`만 반전(다른 설정값은
  그대로 유지)
- [x] `SettingsScreen.kt` — `BuildConfig.DEBUG`로 감싼 "[DEBUG] 프리미엄 켜기/끄기" 버튼을 설정 화면
  최하단(모든 기록 초기화 버튼 아래)에 추가 — **release 빌드에는 노출되지 않음**
- [x] 실기기(Galaxy A25)에서 토글 동작 확인 — "프리미엄 이용 중" 배너 표시, 위젯 테마 4종/체중 기반
  목표 추천/CSV 내보내기 전부 잠금 해제되는 것 스크린샷으로 확인. `uiautomator dump`로 정확한 탭
  좌표를 얻어 안정적으로 자동 조작
- [x] `./gradlew testDebugUnitTest` 통과 확인
- **주의**: `UserSettings.isPremium` 기본값 자체는 다시 `false`로 원복해뒀음(토글 기능만 추가, 기본
  동작은 무료 상태 그대로)

- **배경**: 사용자가 실기기에서 다크 모드로 전환했는데 위젯 배경이 흰색 그대로라고 보고. 조사 결과
  다크모드 감지/위젯 갱신 메커니즘(`WateringApp.kt`의 `ComponentCallbacks2`) 자체는 정상 작동함을
  진단 로그로 확인(앱 프로세스를 껐다 켜면 확실하게 갱신됨) — 다만 **앱을 안 껐다 켠 채로 설정에서만
  다크모드를 토글하면 One UI의 백그라운드 앱 freeze(`FreecessHandler`) 때문에 콜백이 불안정하게
  호출되는 것으로 추정**. 사용자 결정: 지금은 수정하지 않고 "앱 재시작 시 자동 복구"로 둠 — 더 확실한
  브로드캐스트 리시버 방식 보강은 후속 과제로 남김
- **조사 중 발견한 별개 이슈**: 다크 모드에서 1×1 위젯이 목표 달성(100%) 시 배경 전체가 그린으로
  채워지는데, 이는 v0.28.4에서 앱 쪽 목표 달성 색상을 그린→골드로 바꾼 것과 별개로 위젯 쪽
  `WidgetTheme.ACHIEVED_COLOR`가 그린(#34C759)으로 그대로 남아있던 것 — 사용자 요청으로 위젯도
  골드(#F5C860)로 통일
- [x] `core/model/WidgetTheme.kt` — `ACHIEVED_COLOR`를 `#34C759`(그린) → `#F5C860`(골드, 앱과 동일)로 변경
- [x] `./gradlew testDebugUnitTest` 통과, 실기기 재설치 후 앱 재시작으로 위젯 3종(1×1/2×1/2×2)
  전부 골드로 반영됨을 스크린샷으로 확인
- **범위 밖(남은 작업)**: 위젯 `DEFAULT`/`SUNSET`/`MONO`/`NEON` 테마 자체의 파스텔 팔레트 반영은
  이번 범위 밖 — 기존 남은 작업 항목에 계속 추적

## v0.29.0 — 기능 완성도 전수 점검 및 버그 수정 5건

- **배경**: "기능 완성" 선언 이후, 서브에이전트로 전체 코드베이스를 스타일이 아닌 "기능적으로 빠진
  것/버그" 관점에서 점검. TODO/FIXME 주석은 0건(깨끗함), 기존 "완료" 표기 3건 재확인(과장 없음
  확인)했고, 아래 5건의 실제 버그/누락을 발견해 전부 수정
- [x] **다크 모드에 그린이 남아있던 버그**: 오늘(v0.28.4) 목표 달성 색상을 그린→골드로 바꿨는데
  라이트 모드만 반영하고 `Theme.kt`의 `DarkColorScheme.secondary`는 그린(#74C69D) 그대로였음 —
  다크 모드 사용자는 여전히 "싫다"고 하셨던 초록색을 보고 있었음. 골드(#F5C860)로 통일
- [x] **기록 저장 실패 시 크래시 위험 수정**: `WaterDataStore.editSafely()`가 예외를 rethrow하는데
  `HomeViewModel.addWater()`/`addWaterCustom()`의 `viewModelScope.launch{}`에 try/catch가 없어서,
  드물게 저장 실패(디스크 오류 등) 시 앱 전체가 크래시할 수 있었음 → try/catch 추가, 실패 시
  스낵바로 안내(`home_snackbar_record_failed` 신규 문자열, 한/영)
- [x] **스트릭 계산 자정 경계 버그 수정**: `WaterRepository.updateStreak()`가 `LocalDate.now()`(시스템
  시각)로 "오늘"을 재계산하던 것을, `record.dateKey`(레코드 자체의 날짜) 기준으로 바꿈 —
  `AchievementChecker`에서 v0.24.1에 고쳤던 것과 동일한 버그 패턴이 남아있던 것을 발견. 코루틴
  스케줄링 지연/기기 슬립 등으로 실제 시각이 자정을 넘긴 후 이 함수가 호출되면 방금 막 달성한
  기록의 스트릭이 조용히 반영 안 되는 문제였음. `WaterRepositoryTest`의 관련 테스트를 새 동작에
  맞게 갱신(레코드 자체 날짜 기준으로 정상 계산되는지 검증하도록 재작성)
- [x] **위젯 로드 실패 시 폴백 정책 실제 구현**: `CLAUDE.md`에 이미 "위젯 데이터 로드 실패 시 마지막
  성공 데이터를 fallback으로 표시"라고 정책이 적혀 있었는데 실제로는 실패 시 그냥 빈 상태(0/8,
  기본 테마)로 리셋되고 있었음 — 프로세스 내 마지막 성공 상태를 캐시해뒀다가 실패 시 그걸
  fallback으로 쓰도록 `WateringWidgetState.kt`에 `WidgetStateCache` 추가
- [x] **구독 결제 확인(acknowledge) 실패가 조용히 무시되던 것 수정**: `BillingService
  .acknowledgeIfNeeded()`의 콜백이 결과를 완전히 무시하고 있었음(실패해도 로그조차 없음). 확인
  안 된 구매는 Google이 3일 내 자동 환불하는데 진단 흔적이 전혀 안 남는 상황이었음 → 실패 시
  로그 남기도록 수정
- [x] `./gradlew testDebugUnitTest` 전체 통과 확인, 실기기(Galaxy A25) 재설치 후 정상 실행(크래시
  없음) 확인
- **범위 밖**: 다크 모드 골드 색상이 실제 기기에서 육안으로 잘 보이는지는 시스템 다크 모드로
  전환해 직접 확인 필요 — 코드 수정 및 빌드/테스트까지만 완료, 사용자의 육안 재검증 대기

## v0.28.6 — 개인정보처리방침 실제 게시본 갱신 (외부 페이지, 코드 변경 없음)

- **배경**: 문서-코드 정합성 점검(v0.28.5) 중 발견한 이슈의 후속 조치. 실제 게시된 Google Sites
  개인정보처리방침(`sites.google.com/view/watering-privacy`, iOS와 공유)을 확인해보니 "본 앱은
  외부 서버로 어떠한 개인정보도 전송하지 않습니다"라는 문구가 남아있었음 — Firebase
  Crashlytics/Analytics(v0.15)와 백업 기능(Firestore, v0.18)이 이미 구글 서버로 데이터를 보내고
  있어 더 이상 사실이 아니었음(Play 스토어 정책상 실제 리스크)
- [x] 사용자가 직접 Google Sites 페이지를 편집 — "1. 수집하는 정보" 항목에 Crashlytics/Analytics
  자동 수집, 백업(Firestore, 선택 기능), Health Connect 데이터 흐름을 명시하도록 갱신. "5. 알림"의
  "iOS 설정" 문구도 "기기 설정"으로 수정(Android 사용자가 봐도 자연스럽게)
- [x] 게시 후 사용자가 직접 확인 완료
- **범위 밖**: Play Console "Data Safety" 설문에 진단 데이터/앱 활동 데이터 신고는 별도 후속 작업으로
  남음 (남은 작업 참고)

## v0.28.5 — 미사용 위젯 딥링크(quickRecord) 제거 + 문서-코드 정합성 점검

- **배경**: "위젯→앱 딥링크(quickRecord=true) 검증" 남은 작업 항목을 확인하던 중, 정적 코드 분석으로
  이 기능이 실제로는 어디서도 호출되지 않는 죽은 코드임을 발견(사용자 확인 후 제거 결정)
- **발견**: `MainActivity.kt`가 `watering://quick-record` 딥링크를 수신해 `quickRecord: Boolean`을
  `WateringNavGraph` → `HomeScreen`까지 전달하고, `HomeScreen`의 `LaunchedEffect(quickRecord)`가
  자동으로 `viewModel.addWater()`를 호출하는 경로가 구현돼 있었음. 하지만 위젯 3종
  (`CircularWidget`/`RectangularWidget`/`NarrowWidget`) 전부 `actionRunCallback<AddWaterAction>()`
  (앱을 열지 않고 백그라운드에서 즉시 기록)만 사용해 이 딥링크를 실제로 호출하는 지점이 프로젝트
  전체에 없었음 — 아마도 `AddWaterAction`의 백그라운드 즉시 기록 방식이 먼저 도입되며 대체된 이전
  설계의 흔적으로 추정
- [x] `AndroidManifest.xml` — `watering://quick-record` intent-filter 제거
- [x] `MainActivity.kt` — `quickRecord` 감지 로직 및 `WateringNavGraph` 전달 제거
- [x] `WateringNavGraph.kt` — `quickRecord` 파라미터 및 `HomeScreen` 전달 제거
- [x] `HomeScreen.kt` — `quickRecord` 파라미터 및 관련 `LaunchedEffect` 제거
- [x] `./gradlew testDebugUnitTest assembleDebug` 전체 통과 확인
- **문서 정합성 점검 동시 진행**: 서브에이전트로 `CLAUDE.md`/`docs/기획서.md`/`docs/개발보고서.md`
  전수 점검 — Health Connect/Billing/프리미엄 위젯테마·스마트통계·스마트목표 3종이 실제로는 구현
  완료인데 "미구현"으로 남아있던 표기, AGP 버전(8.7.3→8.13.0 누락), 낡은 색상 hex, 개인정보처리방침
  초안에 백업 기능의 Firestore 전송 사실 누락 등을 발견해 함께 수정(커밋 `dcdd1ec`)
- [x] **실기기 재검증 완료(Galaxy A25, 2026-07-04)**: 제거 후 재빌드/재설치해 앱이 정상 실행되고
  크래시 없음을 확인. `adb shell am start -a android.intent.action.VIEW -d "watering://quick-record"`로
  직접 재시도해 `Error: Activity not started, unable to resolve Intent`를 확인 — 딥링크가 실제로
  완전히 제거됐음을 실기기에서 검증

## v0.28.4 — 홈 화면 파스텔 팔레트 확정 + 이모지→벡터 아이콘 전환 (일부)

- **배경**: 사용자가 홈 화면 메인 색상(기존 아쿠아 `#00B4D8`)이 "너무 진하다"고 피드백, 노트북 HTML
  목업으로 여러 파스텔 후보를 실기기에 순서대로 적용해보며 최종 팔레트를 확정. 진행 중 "디자인 관련
  사항은 색상·위치까지 전부 하나씩 승인받고 진행"으로 작업 방식 변경(상세 배경은 세션 대화 참고)
- **팔레트 확정**: `Theme.kt`(라이트 모드)
  - Primary(아쿠아): `#00B4D8` → **`#5DCEE6`** (여러 파스텔 옵션 실기기 비교 후 확정)
  - Secondary(목표 달성 색상): 그린 `#52B788` → **`#F5C860`**(골드) — "달성 시 초록색이 싫다"는
    피드백으로 그린 계열 자체를 포기하고 골드로 전환
  - Tertiary: `#FF9800` → `#FFC078`(파스텔 오렌지)
- **아키텍처 버그 발견 및 수정**: `HomeScreen.kt`에 `Theme.kt`와 별개로 `AquaColor`/`GreenColor`가
  하드코딩돼 있어서, 테마 색상을 바꿔도 진행률 링·"물 마셨어요" 버튼·"목표 달성!" 텍스트가 전혀
  반영되지 않던 문제 발견 → 하드코딩 제거하고 `MaterialTheme.colorScheme.primary`/`secondary`를
  직접 참조하도록 리팩터(단일 소스 오브 트루스로 통합)
- **이모지 → 벡터 아이콘 전환** (플랫폼마다 다르게 렌더링되는 이모지 대신, 테마 색을 따라가는
  아이콘으로 교체):
  - 🔥(연속 기록) → `Icons.Filled.LocalFireDepartment`, tint=tertiary
  - 🏆(최장 연속) → `Icons.Filled.EmojiEvents`, tint=secondary
  - 💧(물방울, 3곳: 빈 기록 화면 아이콘/"물 마셨어요" 버튼/기록 리스트의 물 항목) →
    `Icons.Filled.WaterDrop` (버튼은 텍스트+아이콘 분리, `strings.xml`에서 이모지 제거)
  - 커피/차/주스/우유/기타 이모지는 후보(Material Symbols, Tabler Icons)를 검토했으나 마음에 드는
    스타일을 못 찾아 **보류** — 기존 이모지 그대로 유지
- **디버깅 에피소드**: 아이콘 교체 후 "그대로 이모지로 보인다"는 피드백이 반복돼 클린 빌드까지
  했으나 재현 → 마젠타 색으로 임시 테스트해 실제로는 코드가 정상 작동 중임을 확인. 새 벡터
  아이콘이 원래 이모지와 실루엣·색 계열이 비슷해 육안으로 구분이 어려웠던 것으로 결론(코드 문제
  아님)
- [x] `./gradlew testDebugUnitTest` 전체 통과 확인
- **다음 단계**: 음료별(커피/차/주스/우유/기타) 아이콘 스타일은 미결정 상태로 다음 세션에서 계속
  논의 필요

## v0.28.3 — 실기기 실측 기반 위젯 UI 조정 (1×1 원형, 2×1 좁은)

- **배경**: Galaxy A25(Android 16) 실기기에서 실제 위젯을 눈으로 보며 여러 차례 수정 — 코드만 보고 값을
  추측하는 대신, 매번 빌드·설치·삭제 후 재추가로 실측하며 세부 값을 하나씩 사용자 승인 받아 조정
- **1×1 원형 위젯 (`CircularWidget.kt`)**:
  - 초기 시도(숫자-바 간격 확대, 아이콘/패딩 축소)에서도 위젯 실제 렌더 높이보다 내용물이 커서
    프로그레스바가 아예 안 보이는 문제 지속 → "/n잔" 보조 문구를 제거(아이콘+숫자+바만 남김)해 해결
  - **버그 발견 및 수정**: 안쪽 패딩을 14dp→10dp로 줄이면서 `barWidth` 계산식의 여백 상수(28dp)를
    같이 안 고쳐서, 8/8(100%) 달성 시에도 바가 트랙 끝까지 안 채워지던 버그 발견 → 20dp로 수정
  - 바 위치를 위로 이동(바를 맨 아래로 밀던 `defaultWeight` 스페이서 제거) + 길이 15% 축소(트랙 자체를
    85% 폭으로, 가운데 정렬)
- **2×1 좁은 위젯 (`NarrowWidget.kt`)**:
  - "8 / 8" 숫자 14sp→18sp, "100%" 12sp→14sp로 확대(2×2 위젯과 균형). 고정 텍스트 폭(`CountTextWidth`/
    `PercentTextWidth`)도 46/32dp→52/38dp로 비례 확장해 잘림 없이 반영
  - 텍스트 폭 확장으로 줄어든 바 공간 확보를 위해 요소 간 여백 6dp→4dp로 축소
  - **재조정**: `PercentTextWidth`를 34dp까지 줄였다가 "100%"가 두 줄로 줄바꿈되는 회귀 발견 →
    38dp(4글자가 14sp에서 줄바꿈 없이 들어가는 최소 폭)로 복원, 나머지 절약분은 유지
- [x] `./gradlew testDebugUnitTest` 전체 통과 확인 (위젯 UI 전용 변경이라 회귀 없음)
- **참고**: Glance 위젯은 내용이 실제 위젯 크기를 넘으면 자동 축소 없이 그냥 잘리므로, 작은 위젯의
  요소 크기/개수는 실기기 실측 없이는 안전하게 확정하기 어려움 — 앞으로도 이런 조정은 실기기로
  검증하며 진행할 것

## v0.28.2 — 위젯 기록 시 성취 타이틀 미체크 갭 수정

- **배경**: v0.19에서 문서화된 채 남아있던 알려진 갭 — `AddWaterAction`(위젯 1탭 기록)이
  `achievementChecker.check(...)`를 호출하지 않아, 위젯으로만 기록하는 유저는 성취 타이틀 모달이
  전혀 뜨지 않고 `achievement_unlocked` Analytics 이벤트도 잡히지 않던 문제
- [x] `widget/WateringWidgetState.kt` — `WidgetEntryPoint`에 `achievementChecker(): AchievementChecker`
  추가 (기존 `waterService()`/`analyticsService()`와 동일한 Hilt EntryPoint 패턴)
- [x] `widget/AddWaterAction.kt` — 기록 직전 `waterRepository.todayRecord.first()`로 이전 상태(`prev`)를
  캡처하고, `waterService.updateStreak(...)`의 반환값을 받아 `achievementChecker.check(prev, updated,
  streak, settings.isPremium)` 호출 추가 — `HomeViewModel`이 이미 쓰던 것과 동일한 호출 시그니처
- [x] `./gradlew testDebugUnitTest` 전체 통과 확인 (기존 회귀 없음)
- **범위 참고**: `check()`의 반환값(`Achievement?`)은 `HomeViewModel`처럼 `_pendingAchievement`
  StateFlow에 담아야 풀스크린 모달로 이어지는데, `AddWaterAction`은 위젯 실행 컨텍스트라 이 상태를
  들고 있을 화면이 없어 반환값을 사용하지 않음 — 이번 수정은 `AchievementDataStore` dedup 갱신과
  `achievement_unlocked` Analytics 이벤트 누락만 해결. 위젯 기록으로 새 타이틀을 땄을 때 앱을 열면
  모달로 보여주는 것은 별도 설계(예: pending 상태를 DataStore에 저장해 앱 재진입 시 확인)가 필요한
  후속 작업
- **미검증**: 위젯 탭 시 `achievement_unlocked` Analytics 이벤트가 실제로 잡히는지 실기기 실측
  검증은 다음 세션에서 진행 예정

## v0.28.1 — 잠금화면 위젯 목록 미노출 버그 수정

- **배경**: Galaxy A25(Android 16, One UI)로 최초 실기기 USB 테스트 진행 중 발견. 홈 화면 위젯 3종은
  정상 배치·갱신되지만, 잠금화면 위젯 추가 목록에는 "워터링" 앱 자체가 아예 노출되지 않는 문제 확인
- **원인 분석**: `adb logcat` 실시간 필터링(패키지명 기준, 시스템/런처 노이즈 제외)으로 위젯 선택
  미리보기 단계에서 세 위젯(원형/좁은/직사각형) 전부 `W/AppWidgetHostView: Error inflating AppWidget`
  경고가 재현되는 것을 확인. 함께 찍힌 `androidx.glance.oneui.common.AppWidgetManagerKt.getTemplatePreview`
  예외 스택과 `HoneySpace.TemplatePreviews`의 `initialLayoutId : 0` 로그로, 세 `widget_info.xml` 모두
  `android:initialLayout`이 선언돼 있지 않아(0으로 처리) One UI의 Glance 위젯 템플릿 미리보기 생성이
  실패하고 있음을 특정. 홈 화면 피커는 이 실패를 관대하게 처리해 빈 미리보기로라도 노출하지만,
  잠금화면 피커는 실패 시 목록에서 아예 제외하는 것으로 추정
- [x] `res/xml/circular_widget_info.xml`/`rectangular_widget_info.xml`/`narrow_widget_info.xml` 3개
  파일에 `android:initialLayout="@layout/glance_default_loading_layout"` 추가 — Jetpack Glance
  라이브러리가 기본 제공하는 리소스를 참조하는 것이라 신규 디자인 에셋 제작 불필요. `Error inflating
  AppWidget` 경고의 실제 원인이었던 정당한 버그 수정으로 그대로 유지
- [x] `./gradlew installDebug` 재빌드/재설치 성공 확인 (리소스 참조 유효성도 빌드 성공으로 함께 확인)
- **재검증 결과 — 잠금화면 미노출은 별개의 플랫폼 제약으로 결론**: 수정 후에도 실기기에서 여전히
  잠금화면 위젯 목록에 앱이 뜨지 않아 추가 조사 진행. 앱 삭제 시 로그에 찍힌 삼성 자체 컴포넌트
  `SharedWidgetListManager`/`SharedAppWidgetFacade`(size=33)로 보아 One UI가 구글 표준 위젯 피커와
  별도로 자체 "공유 위젯 목록" 시스템을 운영 중임을 확인. 실기기에서 잠금화면 길게 눌러 위젯 추가
  화면을 직접 확인한 결과, **갤러리/날씨/디바이스 케어/리마인더 등 삼성 자사(1st-party) 앱만 노출되고
  서드파티 앱은 하나도 뜨지 않음**을 확인 — 즉 이 기기의 One UI 8(Android 16) 잠금화면 위젯 피커는
  현재 삼성 자사 앱 화이트리스트로만 운영되며, 매니페스트/Glance 설정을 아무리 수정해도 우회 불가능한
  **플랫폼(OEM) 제약**으로 결론. `docs/기획서.md` 7-6절 리스크 표에 이미 "잠금화면 위젯 제조사별 지원
  편차"로 예견돼 있던 상황과 정확히 일치
- **다음 단계**: 코드 수정으로 해결 가능한 사안이 아님 — 삼성 소프트웨어 업데이트로 서드파티 지원이
  열리는지 추적하거나, Pixel 등 다른 제조사 기기로 재검증 필요 (기획서 리스크 대응 방안대로 미지원
  기기 안내 문구 추가는 후속 검토)

## v0.27 — 연속 기록 마일스톤 확장 (프리미엄 재기획 4순위)

- **배경**: 프리미엄 재기획 4순위(기획서 8-2절). 기존 "연속 기록 보호권"(월 1회, 이미 구현 완료)은
  그대로 유지하면서, 60/100/365일 연속 달성 시 프리미엄 전용 마일스톤 타이틀을 추가해 구독을
  지속적으로 체감하게 함. 3순위(스마트 목표 추천 + 음료별 환산율, Phase 1~4)가 v0.26으로 완료된
  직후 착수
- **구현 내용**:
  - `Achievement` enum에 `isPremiumOnly: Boolean = false` 필드 추가(기존 `isStreakBased`와 동일 패턴,
    기존 6개 항목은 무변경으로 동작) + `STREAK_60`(💎 "두 달의 기적") / `STREAK_100`(🌟 "백일의 전설") /
    `STREAK_365`(🎇 "1년의 레전드") 3개 신규 항목 추가
  - `AchievementChecker.check()`에 `isPremium: Boolean = false`(기본값)를 4번째 파라미터로 추가 —
    기존 3-arg 호출/테스트 호환성 유지. `when` 블록에 `isPremium && streak.currentStreak >= 365/100/60`
    분기를 기존 30/7/3 위에 추가. 비프리미엄 유저가 60일 이상 연속 달성해도 `isPremium` 조건이
    false라 자연스럽게 `>= 30` 분기로 떨어지고, 이미 STREAK_30을 획득한 상태라 `emit`이 null →
    GOAL_ACHIEVED로 대체되는 기존 동작이 그대로 유지됨(별도 캡 로직 불필요)
  - `HomeViewModel.addWater`/`addWaterCustom` 두 곳에서 `achievementChecker.check(...)` 호출 시
    이미 스코프에 있던 `current.settings.isPremium`을 4번째 인자로 전달
  - `strings.xml`(한국어) / `values-en/strings.xml`(영문) 양쪽에 신규 타이틀·메시지 6개 문자열과
    `feature_streak_protection_description` 문구 확장(60·100·365일 마일스톤 언급 추가) 반영
  - `AchievementDialog.kt`는 enum별 분기 없이 `emoji`/`titleRes`/`messageRes`를 범용 렌더링하므로
    UI 코드 변경 없이 신규 타이틀이 자동 반영됨
  - `WaterRepository.updateStreak`(보호권 로직)와 `dismissAchievement()`의 STREAK_7 리뷰 요청 분기는
    범위 밖으로 무변경
- **테스트**: `AchievementCheckerTest`에 9개 추가(프리미엄 시 STREAK_60/100/365 반환 3개, 비프리미엄
  시 STREAK_30 제한 3개, 이미 획득 시 GOAL_ACHIEVED 대체 3개) — 전체 165개 중 165개 통과
- **미검증**: 60/100/365일을 실제로 기다릴 수 없어 에뮬레이터 실측은 StreakInfo 값을 임시로
  주입하는 방식으로만 확인 예정(단위 테스트로 로직 정확성은 검증 완료)
- **다음 단계**: 프리미엄 재기획 5순위(기록 CSV 내보내기) — 아직 착수 전

## v0.26 — 물 마시기 기록 Health Connect 자동 저장 (프리미엄 재기획 3순위, Phase 4/4— 무료)

- **배경**: 프리미엄 3순위 4단계 중 마지막 단계이자 유일한 무료 기능(기획서 5-6절). 사용자가
  "물 마셨어요"를 탭하거나 위젯에서 기록할 때마다 Health Connect에도 `HydrationRecord`로
  자동 저장. `UserSettings.healthConnectEnabled`(필드만 있고 계속 미사용 상태였음)를 이번에
  처음 실사용. Phase 2/3와 달리 화면이 요청하는 일회성 동작이 아니라 설정에서 켜고 끄는
  지속적 동기화라 별도 토글 UI로 구현
- [x] `core/service/HealthConnectService.kt` — `writeHydrationRecord(volumeMl, timestampMillis)`
  추가. `HYDRATION_PERMISSIONS`를 read+write에서 **write 전용**으로 축소(수분 데이터를
  Health Connect에서 읽어올 계획이 없어 불필요한 권한 요청 제거) — `AndroidManifest.xml`의
  `READ_HYDRATION` 선언도 함께 제거
- [x] `core/service/WaterService.kt` — `addWater()`가 기록 직후 연동 켜짐+권한 있음이면
  `entry.amount * entry.drinkType.hydrationRate`(v0.23과 동일한 환산 근거)로 best-effort
  저장. 실패해도 로컬 기록/위젯 갱신에는 영향 없음(try-catch + 로그만 남김) — 실측 검증 중
  실제로 이 안전장치가 정상 작동함을 확인(아래 버그 참고)
  - `HomeViewModel`(홈 버튼 + RecordSheet 커스텀 기록)과 위젯 `AddWaterAction`이 전부
    `WaterService.addWater()`로 모이므로 호출부 수정 없이 세 진입점 모두 자동 적용
- [x] `features/settings/SettingsViewModel.kt`/`SettingsScreen.kt` — "건강 연동" 섹션에
  `Auto-Save to Health Connect` 토글 추가(무료라 프리미엄 잠금 없음). 켤 때 SDK 가용성 확인 →
  권한 있으면 즉시 저장, 없으면 권한 요청 → 승인/거부에 따라 상태 반영(Phase 3의
  `WeightGoalUiState` 패턴과 동일한 구조의 `HydrationSyncUiState` 추가)
- [x] **실측 중 발견한 버그**: `HydrationRecord`는 `startTime`이 `endTime`보다 **반드시**
  이전이어야 함(같으면 `IllegalArgumentException`). 처음엔 시작=종료 시각(순간 기록)으로
  구현했다가 에뮬레이터 실측에서 예외 발생 확인 → 종료 시각을 1ms 뒤로 조정해 수정
- [x] 단위 테스트 곳곳에서 `android.util.Log` 호출이 순수 JUnit 환경에서 "not mocked"
  예외를 던지는 문제 발견 → `app/build.gradle.kts`에 `testOptions.unitTests.isReturnDefaultValues = true`
  추가(표준적인 해결 방식, Robolectric 불필요)
- [x] 단위 테스트 10개 추가/수정(`WaterServiceTest` 4개, `SettingsViewModelTest` 6개,
  `HealthConnectServiceTest` 권한 개수 검증 수정) — 전체 156개 통과
- [x] 에뮬레이터(Pixel_8, Android 14) 실측 검증 완료 — 설정에서 토글 ON → 시스템 권한 화면에
  "Hydration" 쓰기 권한만 요청됨(읽기 없음) 확인 → 승인 → 홈에서 "Drink water" 2회 탭 →
  Health Connect의 "Data and access → Nutrition → Hydration"이 데이터 없음에서 데이터
  있음으로 바뀐 것으로 저장 확인, 로그캣에 오류 없음
- **프리미엄 3순위(스마트 목표 추천 + 음료별 수분 환산율) 4단계 전체 완료**: Phase 1(v0.23) →
  Phase 2(v0.24) → Phase 3(v0.25) → Phase 4(v0.26)

## v0.25 — 체중 기반 스마트 목표 추천 (프리미엄 재기획 3순위, Phase 3/4)

- **배경**: Phase 2(v0.24)에서 만든 `HealthConnectService`를 처음 실사용하는 단계. Health
  Connect에 기록된 최신 체중을 읽어와 "체중 1kg당 30~35ml" 가이드라인으로 하루 목표 잔 수를
  추천. 기획서 원칙대로 다이어트/체중감량 프레이밍 없이 순수 편의 기능으로만 제시하고,
  권한을 거부해도 다른 기능엔 전혀 영향 없음을 다이얼로그 문구로도 명시
- [x] `core/service/HealthConnectService.kt` — `readLatestWeightKg(): Double?` 추가
  (`ReadRecordsRequest` + `TimeRangeFilter.before(now)`로 최신 체중 1건만 조회)
- [x] `core/service/StatsInsightService.kt` — `recommendedGoalCups(weightKg, cupSizeMl)` 순수
  함수 추가 (체중×33ml÷컵크기, 1~20잔으로 클램프 — 기존 목표 잔 수 UI 허용 범위와 동일)
- [x] `features/settings/SettingsViewModel.kt` — `WeightGoalUiState` sealed 상태
  (Idle/Loading/NeedsPermission/PermissionDenied/NotAvailable/NoWeightData/Recommended) +
  권한 확인 → 요청 → 체중 읽기 → 추천 계산 흐름
- [x] `features/settings/SettingsScreen.kt` — "기록 설정" 섹션에 프리미엄 게이팅된
  "체중 기반 목표 추천받기" 행 추가(기존 `WidgetThemeSetting` 잠금 패턴 재사용), 권한 요청
  런처 + 상태별 다이얼로그(권한 필요/거부/Health Connect 미설치/체중 데이터 없음/추천 결과)
- [x] `AndroidManifest.xml` — Health Connect 권한 설명(rationale) 인텐트 필터 수정: 처음
  넣었던 `androidx.health.connect.action.SHOW_PERMISSIONS_RATIONALE`는 실제로 존재하지 않는
  액션이었음(system Health Connect 컨트롤러 앱을 디컴파일해 확인) — 실제로는
  `android.intent.action.VIEW_PERMISSION_USAGE` + 카테고리 `android.intent.category.HEALTH_PERMISSIONS`
  조합이어야 하며, `MainActivity.kt`의 인텐트 분기도 함께 수정
- [x] `PremiumScreen.kt` — 구현 완료로 `PREMIUM_FEATURES`에 "🎯 스마트 목표 추천" 추가
- [x] 단위 테스트 9개 추가(`StatsInsightServiceTest` 4개: 계산/컵크기별/최소·최대 클램프,
  `SettingsViewModelTest` 5개: 상태 전이 전체 케이스) — 전체 144개 전원 통과
- [x] 에뮬레이터(Pixel_8, Android 14) 실측 검증 완료 — Health Connect가 OS에 내장돼 있어 별도
  설치 불필요. 권한 요청→거부(PermissionDenied)→재요청→승인→체중 데이터 없음(NoWeightData)
  경로 전부 실기기에서 확인. "추천됨" 경로는 임시로 쓰기 권한 부여 + 테스트 체중(66.0kg) 삽입
  으로 검증(66kg×33÷200ml=10.89→11잔, 실제 표시·적용까지 확인) 후 임시 코드 전부 원복
  (`isPremium` 기본값 임시 true 전환도 동일하게 원복)
- **다음 단계(Phase 4)**: 물 마시기 기록 → Health Connect `HydrationRecord` 자동 저장(무료) —
  아직 착수 전, 사용자 승인 후 진행 예정

## v0.24.1 — AchievementChecker 날짜 하드코딩 버그 수정

- **배경**: v0.24 작업 중 `AchievementCheckerTest` 2건이 실패하는 것을 발견. 원인은
  `AchievementChecker.check()`가 인자로 받은 `DayRecord.dateKey`를 쓰지 않고
  `LocalDate.now()`(실제 시스템 시각)로 날짜 키를 다시 계산하던 것 — 테스트가 레코드에 박아둔
  고정 날짜(`"2026-07-02"`)와 실행 시점의 실제 날짜(`"2026-07-03"`)가 어긋나면서 mock이
  매칭되지 않아 실패. 테스트만의 문제가 아니라, 자정 경계에서 기록이 만들어진 시점과
  `check()` 호출 시점의 날짜가 달라지면 실제 앱에서도 업적이 잘못된 날짜로 기록될 수 있는
  잠재 버그였음
- [x] `core/service/AchievementChecker.kt` — `LocalDate.now()` 기반 계산 제거, `next.dateKey`
  (레코드 자체의 날짜) 사용하도록 수정. 미사용 `LocalDate`/`DateTimeFormatter` import·필드 제거
- [x] 전체 단위 테스트 135개 전원 통과 확인 (기존 실패 2건 포함)

## v0.24 — Health Connect 인프라 구축 (프리미엄 재기획 3순위, Phase 2/4)

- **배경**: v0.23(Phase 1) 다음 단계로, Phase 3(체중 읽기 → 스마트 목표 추천)과 Phase 4(물 마시기
  기록 → HydrationRecord 자동 저장)가 공통으로 쓸 Health Connect 연동 기반만 먼저 구축. 이번
  단계에서는 어떤 화면도 이 서비스를 호출하지 않음 — UI 연동은 Phase 3/4에서 진행
- **버전 카탈로그 오류 수정**: `gradle/libs.versions.toml`의 `healthConnect = "1.0.1"`은 실제로
  존재하지 않는 버전(Health Connect는 1.0 정식 출시 없이 1.0.0-alpha11에서 바로 1.1.0으로
  넘어감)이었음을 발견해 `1.1.0`(최신 안정 버전)으로 수정
- **AGP/Gradle 업그레이드**: Health Connect 1.1.0은 AGP 8.9.1 이상을 요구해 기존 AGP 8.7.3과
  충돌. 메이저 버전 9.x(현재 stable)까지 건너뛰는 대신, 같은 8.x 라인의 최신 안정판인 **AGP
  8.13.0**으로 상향(사용자 확인 후 결정) — compileSdk 36을 정식 지원해 기존에 있던 "AGP가
  compileSdk 36까지 테스트되지 않음" 경고도 함께 해소됨. Gradle wrapper도 8.9 → 8.13으로 동반
  업그레이드(AGP 8.13.0의 최소 요구 버전)
- [x] `app/build.gradle.kts`에 `implementation(libs.health.connect)` 추가
- [x] `AndroidManifest.xml` — `READ_HYDRATION`/`WRITE_HYDRATION` 권한 실제 선언(기존 주석 처리된
  줄 교체), Phase 3에서 쓸 `READ_WEIGHT`도 미리 선언(재심사 방지), `MainActivity`에 Health
  Connect 권한 설명(rationale) 인텐트(`SHOW_PERMISSIONS_RATIONALE`) 필터 추가
- [x] `MainActivity.kt` — rationale 인텐트 수신 시 기존 개인정보처리방침 URL
  (`R.string.privacy_policy_url`)을 브라우저로 열고 종료하는 분기 추가
- [x] `core/service/HealthConnectService.kt` 신규 — SDK 가용성 확인(`availability`,
  `mapAvailability()` 순수 함수로 분리), 권한 상수(`HYDRATION_PERMISSIONS`/`WEIGHT_PERMISSIONS`),
  `hasPermissions()`, `permissionRequestContract()` 제공. `HealthConnectClient`는 SDK 사용
  가능할 때만 lazy 생성해 미설치 기기에서 크래시 방지
- [x] `di/ServiceModule.kt` — `provideHealthConnectService()` 추가
- [x] 단위 테스트 5개 추가(`HealthConnectServiceTest`: 가용성 매핑 3개, 권한 상수 2개) — 전체
  135개 중 133개 통과, 실패 2건은 기존 `AchievementCheckerTest` 버그(무관, 날짜 하드코딩값
  "2026-07-02"가 시스템 날짜(2026-07-03)와 어긋나 발생, v0.18 이전부터 존재)
- [x] `./gradlew assembleDebug` 성공 확인 — 이번 단계는 UI 연동이 없어 에뮬레이터 스크린샷
  검증은 생략(Phase 3/4에서 화면 연동 시 진행)
- **다음 단계(Phase 3)**: 체중 읽기 → 스마트 목표 추천(프리미엄, 다이어트 프레이밍 배제) —
  아직 착수 전, Phase 2 완료 후 사용자 승인 필요

## v0.23 — 음료별 수분 환산율 정보성 지표 (프리미엄 재기획 3순위, Phase 1/4)

- **배경**: 프리미엄 재기획 3순위("스마트 목표 추천 + 음료별 수분 환산율")는 Health Connect 연동까지
  포함해 범위가 커서, 한 세션에 몰아서 구현하지 않고 4단계로 나눠 진행하기로 계획(상세 계획은 세션
  플랜 파일 참고). 이번엔 Health Connect가 필요 없는 **Phase 1(음료별 수분 환산율 정보성 지표)**만
  구현
- **설계 결정**: `DrinkType.hydrationRate`(물 100%/커피 70%/차 90%/주스 85%/우유 88%/기타 80%)는
  기존에 필드만 존재하고 앱 어디서도 읽지 않던 죽은 값이었음. 이번에 처음 사용하되, 기존
  `DayRecord.totalCount`/`isAchieved`/스트릭/위젯/알림이 전부 의존하는 "잔 수" 기반 달성 계산은
  전혀 건드리지 않고, SmartStatsScreen에 "오늘 실제 수분 섭취량: 약 XXXml"이라는 **정보성 지표만
  추가**하기로 결정(회귀 위험 없는 완전 추가형 변경)
- [x] `core/service/StatsInsightService.kt` — `calculateHydrationVolumeMl(entries): Int` 추가
  (entry.amount × drinkType.hydrationRate 합산 후 반올림, 순수 함수)
- [x] `features/stats/SmartStatsViewModel.kt` — `SmartStatsUiState.todayHydrationVolumeMl` 필드 추가
- [x] `features/stats/SmartStatsScreen.kt` — "30일 트렌드" 섹션 하단에 `HydrationVolumeCard` 추가
  (이미 프리미엄 게이팅된 화면이라 별도 잠금 UI 불필요)
- [x] `values/strings.xml` + `values-en/strings.xml` — 신규 문구 1개
- [x] 단위 테스트 5개 추가(`StatsInsightServiceTest` 4개: 물만/혼합/반올림/빈 리스트,
  `SmartStatsViewModelTest` 1개: 물+커피 혼합 340ml 계산) — 전체 130개 중 128개 통과, 실패 2건은
  기존 `AchievementCheckerTest` 버그(무관, v0.18 이전부터 존재)
- [x] 에뮬레이터 실측 검증 — 물 200ml + 커피 200ml 기록 후 SmartStats 화면에 "오늘 실제 수분
  섭취량: 약 340ml" 정확히 표시됨(200×1.0 + 200×0.7 = 340 기댓값과 일치) 확인. 검증 후 임시
  `isPremium=true` 원상복구
- **다음 단계(Phase 2)**: Health Connect 인프라 구축(의존성 추가, 권한, `HealthConnectService`
  래퍼) — 아직 착수 전, 사용자 승인 후 진행 예정

## v0.22 — 스마트 통계: 30일 트렌드 + 시간대별 인사이트 + 연간 기록 (프리미엄 재기획 2순위)
- **배경**: 프리미엄 재기획 2순위. `StatsScreen`은 7일 통계만 제공해 무료/프리미엄 구분이 없었음 —
  통계 탭을 열 때마다 매번 새로운 발견(30일 추세, 잘 놓치는 시간대, 1년 기록)을 제공해 구독 가치를
  강화. 최초엔 연간 통계를 데이터 용량 우려로 별도 세션에 미루자고 제안했으나, 사용자 요청으로 이번
  범위에 함께 포함
- **데이터 조사 결과**: `WaterDataStore`가 이미 최근 90일치 `DayRecord`(엔트리 타임스탬프 포함)를
  보관하고 있어 30일 트렌드·시간대별 인사이트는 데이터 레이어 변경 없이 구현 가능했음. 연간(365일)
  통계만 신규 저장소가 필요했음(엔트리 전체를 365일치 JSON blob으로 늘리면 Preferences DataStore
  쓰기 지연 우려가 있어, 일별 집계만 담는 경량 병행 저장소로 분리)
- [x] `core/model/DailyAchievement.kt` (신규) — 연간 히트맵용 경량 일별 집계
  (`dateKey, totalCount, goal` — `DayRecord`와 달리 엔트리 리스트 없음)
- [x] `core/datastore/WaterDataStore.kt` — `ANNUAL_HISTORY` Preferences 키 추가, 기존
  `archiveTodayToHistory()`(90일 상세 이력)와 같은 호출 지점에서 `archiveToAnnualHistory()`가 함께
  실행되도록 추가(`.take(365)` 트림). 기존 90일 로직은 무변경 — 완전히 병행하는 별도 저장소
- [x] `core/data/WaterRepository.kt` — `getAnnualHistory()` 패스스루 추가
- [x] `core/service/StatsInsightService.kt` (신규, DI 불필요한 순수 object) — 최근 30일 이력을
  사용자의 알림 활성 시간대 내에서 2시간 단위 버킷으로 집계, 최솟값 버킷이 전체 평균의 60% 미만일
  때만 "자주 놓치는 시간대"로 판단(억지 패턴 방지). 기록일 7일 미만이거나 활성 시간대가 6시간
  미만(버킷 3개 미만)이면 `null` 반환 → UI는 "데이터가 더 필요해요" 플레이스홀더. 엣지 케이스
  전수 테스트(`StatsInsightServiceTest`, 7개) — 정확히 7일/6시간 경계값, 버킷 균일, 엔트리 없음 등
- [x] `features/stats/BarChartRow.kt` (신규) — 기존 `StatsScreen`의 `WeekBarChart`(7개 막대,
  라벨 있음)에서 막대 드로잉 로직을 `BarChartRow(entries, columnWidth, barWidth, ...)` 공용
  Composable로 추출. `WeekBarChart`는 이 위의 얇은 래퍼로 리팩터(기존 7일 차트 렌더링 결과 무변경
  확인), 신규 `MonthBarChart`(30개 얇은 막대, 라벨 없음)가 동일 프리미티브 재사용
- [x] `features/stats/SmartStatsViewModel.kt` + `SmartStatsScreen.kt` (신규) — 기존 `stats/`
  패키지에 co-locate. `getHistory()`(30일 트렌드/인사이트) + `getAnnualHistory()`(히트맵) +
  `userSettings`를 조합. 화면 구성: 30일 트렌드 차트 + 인사이트 카드 → 연간 히트맵
  (GitHub 컨트리뷰션 그래프 스타일, 7행×약 52열, 달성 여부에 따라 색상 단계 구분)
- [x] `features/stats/StatsScreen.kt` — 하단에 프리미엄 게이팅 CTA 카드 추가, 기존
  `WidgetThemeSetting`이 확립한 잠금 패턴(dim + 자물쇠 아이콘 + 안내 문구) 재사용. 프리미엄 유저는
  탭하면 `SmartStatsScreen`으로, 무료 유저는 프리미엄 화면으로 이동
- [x] `navigation/WateringNavGraph.kt` — `Screen.SmartStats` 라우트 추가
- [x] `features/premium/PremiumScreen.kt` — `PREMIUM_FEATURES`에 "스마트 통계" 항목 추가
  (구현·검증 완료 후에만 추가하는 기존 원칙 유지)
- [x] `values/strings.xml` + `values-en/strings.xml` — 화면 제목, 인사이트 문구 템플릿(24시간제
  숫자만 사용해 오전/오후 로케일 포맷팅 문제 회피), 데이터 부족 플레이스홀더, CTA 카드 문구 등 신규 키
- [x] `WaterRepositoryTest.kt`에 `getAnnualHistory` 패스스루 테스트 추가. 전체 125개 테스트 중
  123개 통과(`./gradlew testDebugUnitTest`), 실패 2건은 기존 `AchievementCheckerTest` 버그(v0.19
  이전부터 존재, 이번 변경과 무관). `./gradlew assembleDebug` 빌드 성공
- **에뮬레이터 실측 검증 중 발견 및 수정한 버그**: 30일 차트·연간 히트맵 둘 다 30개/365개 항목이
  카드 폭보다 넓어 가로 스크롤이 필요한데, 기본 스크롤 위치가 항상 맨 왼쪽(가장 오래된 날짜)이라
  정작 가장 중요한 "오늘" 막대·칸이 스크롤 없이는 화면 밖에 가려져 있었음(데이터 자체는 정확했음 —
  `Log`로 ViewModel 상태를 직접 확인해 데이터 문제가 아님을 먼저 확인한 뒤 원인 특정). `MonthBarChart`/
  `AnnualHeatmap` 양쪽에 `LaunchedEffect` + `snapshotFlow { scrollState.maxValue }`로 최초 진입 시
  자동으로 끝까지(최신 날짜 쪽) 스크롤되도록 수정, 재검증 완료
- **후속 개선(사용자 질문 계기)**: "일주일 정도 쓴 사람은 인사이트가 안 나오나?" 질문을 계기로 확인한
  결과, `MIN_DAYS_WITH_DATA=7`이라 날짜 수 조건 자체는 충족해도 시간대별 편차가 뚜렷하지 않으면(고르게
  마시는 습관) 여전히 `null` — 이 경우와 "기록일 자체가 부족한 경우"가 똑같이 "데이터가 더 필요해요"
  문구로 뭉뚱그려져 이미 규칙적인 사용자에게 오해를 줄 수 있었음. `StatsInsightService`의 반환 타입을
  `TimeOfDayInsight?` → `sealed interface TimeOfDayInsightResult`(`Found`/`InsufficientData`/
  `NoClearPattern`)로 변경해 두 경우를 분리, `NoClearPattern` 전용 문구("특별히 놓치는 시간대가
  없어요. 고르게 잘 마시고 있어요!") 추가. `StatsInsightServiceTest`/`SmartStatsViewModelTest` 갱신,
  전체 테스트 재통과 확인(125개 중 123개, 무관한 기존 실패 2건 동일)
- [x] 프리미엄 게이팅 무료/프리미엄 상태 전환 에뮬레이터 실측 검증 완료 — 무료 상태에서 CTA 카드
  잠금 표시·탭 시 프리미엄 이동 확인, `UserSettings.isPremium` 기본값을 임시로 true로 바꿔 재현한
  프리미엄 상태에서 실제 물 마시기 기록 → 30일 차트·연간 히트맵에 실시간 반영되는 것까지 확인
  (검증 후 임시 변경 원상복구, `git diff`로 의도한 변경분만 남았는지 확인)
- **범위 밖(후속 작업)**: iOS `PremiumView.features` 동시 반영(오늘도 Android만), 히트맵 탭
  인터랙션(날짜별 상세 팝업), Play Console 실등록 무관

## v0.21 — 위젯 프리미엄 테마 (프리미엄 재기획 1순위)
- **배경**: 2026-07-03 프리미엄 재기획(경쟁앱 비교 + 구독 이탈 방지 리서치, `docs/기획서.md` 8-2 참고)의
  1순위 항목. 기존 유일한 프리미엄 기능(연속 기록 보호권)은 월 1회 이하로만 체감돼 "매일 느끼는
  구독 가치"가 없었음 — 하루 80회 이상 노출되는 잠금화면/홈화면 위젯에 프리미엄 전용 색상 테마를
  붙여 이 문제를 해소
- [x] `core/model/WidgetTheme.kt` (신규) — `DrinkType`/`Achievement`와 동일한 패턴의
  `@Serializable enum class WidgetTheme(@StringRes displayNameRes, accentColor: Color)`. 4종:
  기본(아쿠아, 무료 기본값)/선셋(코랄)/모노(차콜)/네온(마젠타). 목표 달성 시 색상(그린)은 테마와
  무관하게 항상 고정(성공 신호 일관성). `companion object.ACHIEVED_COLOR`를 위젯 3종이 공유
- [x] `core/model/UserSettings.kt` — `widgetTheme: WidgetTheme = WidgetTheme.DEFAULT` 필드 추가
  (기본값 있어 기존 DataStore/Firestore 백업 데이터와 완전 하위 호환)
- [x] `widget/WateringWidgetState.kt` — `WidgetState.theme` 추가, `loadWidgetState()`에서
  `if (settings.isPremium) settings.widgetTheme else WidgetTheme.DEFAULT`로 게이팅. 구독이
  만료돼도 선택했던 테마는 저장된 채로 두고 위젯만 기본 테마로 조용히 복귀(재구독 시 즉시 복원)
- [x] `widget/CircularWidget.kt`/`RectangularWidget.kt`/`NarrowWidget.kt` — 3개 파일에 각각
  하드코딩돼 있던 아쿠아(`#00B4D8`)/그린(`#34C759`) accent 색상을 `state.theme.accentColor`/
  `WidgetTheme.ACHIEVED_COLOR` 참조로 교체 — 3중 중복 하드코딩 제거를 겸함. 배경 다크/라이트
  자동 전환 로직은 테마와 무관하게 그대로 유지(스코프를 accent 색상에만 한정)
- [x] `features/settings/SettingsViewModel.kt` — `updateWidgetTheme()` 추가, 기존 `updateDailyGoal`과
  동일 패턴으로 변경 즉시 `widgetUpdater.updateAll()` 호출
- [x] `features/settings/SettingsScreen.kt` — 프리미엄 배너 바로 다음에 "위젯 테마" 섹션 신규
  (`WidgetThemeSetting`). 원형 스와치 4개, 프리미엄 유저는 탭해서 선택(선택 시 테두리 강조), 무료
  유저는 스와치에 잠금 아이콘 오버레이 + "프리미엄에서 잠금 해제" 안내, 탭하면 프리미엄 화면으로 이동
- [x] `features/premium/PremiumScreen.kt` — `PREMIUM_FEATURES`에 "위젯 테마" 항목 추가(기존
  `PremiumFeature(icon, titleRes, descriptionRes)` 패턴 그대로, 이번에 실제 구현 완료 상태로 추가)
- [x] `values/strings.xml` + `values-en/strings.xml` — 테마 이름 4종, 섹션 제목, 잠금 안내 문구,
  프리미엄 기능 설명 신규 키 추가
- [x] `SettingsViewModelTest.kt`에 `updateWidgetTheme_설정을저장하고위젯을갱신한다` 테스트 추가
  (`widgetUpdater.updateAll()` 호출 검증, 기존 `updateDailyGoal` 테스트와 동일 패턴)
- [x] 전체 112개 테스트 중 110개 통과(`./gradlew testDebugUnitTest`), `./gradlew assembleDebug` 빌드
  성공. 실패 2건은 기존 `AchievementCheckerTest` 버그(v0.19 이전부터 존재, 오늘 변경과 무관)
- [x] **에뮬레이터 실측 검증 완료** — 무료 상태에서 스와치 잠금 표시·프리미엄 화면 이동 확인,
  `UserSettings.isPremium` 기본값을 임시로 `true`로 바꿔 재빌드해 프리미엄 상태 재현(실기기 결제
  불가한 에뮬레이터 환경 한계 우회) 후 선셋 테마 선택 → 홈 화면에 배치된 위젯 3종(원형/좁은/
  직사각형) 모두 실시간으로 코랄 색상 반영되는 것을 스크린샷으로 확인. 검증 후 임시 변경 원상복구,
  `git diff`로 의도한 `widgetTheme` 필드 추가분만 남았는지 확인
- **범위 밖(후속 작업)**: iOS `PremiumView.features`에 동일 기능 미반영(플랫폼 UI 용어 일치 규칙상
  필요하나 오늘은 Android만), Play Console 상품 미등록 상태라 실제 구매 후 테마 잠금 해제는 여전히
  실기기 검증 불가(기존에 알려진 제약과 동일 성격)

## v0.20 — 영문판(English) 지원
- **배경**: iOS는 이미 한국어/영어 완비인데 Android는 `values-en/`이 아예 없어 "글로벌 확장" 목표와
  배치되는 상태(2026-07-02 남은 작업에 우선순위로 등록). 조사 결과 `values/strings.xml`엔 리소스가
  19개뿐이고 그중 5개만 실제로 `stringResource()`로 연결돼 있어, 화면·ViewModel·위젯 전반에 약 110개의
  한국어 문자열이 Kotlin 코드에 리터럴로 박혀 있었음 — `values-en/`만 추가해서는 거의 번역되지 않는
  상태였기 때문에 전체 문자열 추출부터 진행
- [x] `core/model/Achievement.kt` — `title`/`message`(String) → `titleRes`/`messageRes`(`@StringRes Int`)로
  교체. 순수 enum이라 `stringResource()`를 직접 호출할 수 없어, 실제 텍스트 해석은 항상 호출부
  (Composable에서 `stringResource(achievement.titleRes)`)에서 수행
- [x] `core/model/DrinkType.kt` — `displayName`(String, 하드코딩) → `displayNameRes`(`@StringRes Int`)로
  교체. 그동안 죽어있던 `labelResKey: String` 필드(아무도 참조하지 않던 중복 메커니즘) 완전히 제거
- [x] `features/home/HomeViewModel.kt`, `features/settings/BackupViewModel.kt`,
  `features/stats/StatsViewModel.kt` — Composable이 아니라 `stringResource()`를 못 쓰는 ViewModel 3곳에
  `@ApplicationContext Context`를 생성자 주입 (`WaterService`/`NotificationService`/`BillingService`가
  이미 쓰던 패턴 재사용)하여 스낵바 메시지·에러 메시지·요일 라벨을 `context.getString(...)`으로 해석
- [x] `features/premium/PremiumScreen.kt`의 `pricing()` 함수 리팩터 — 원래 `@Composable`이 아닌 순수
  함수 안에서 "%d일 무료체험 후 %s" 같은 최종 문자열을 직접 조립해 `stringResource`를 호출할 수 없었음.
  `PlanPricing`이 원본 값(trialDays/price/suffix)만 반환하도록 바꾸고, 최종 문자열 조립은
  `priceLineText()`(`@Composable` 확장 함수)에서 수행하도록 분리
- [x] `core/service/BillingService.kt` — 상품 로드 실패 에러 메시지 리소스화 (이미 Context 보유, 한 줄 수정)
- [x] `widget/RectangularWidget.kt`, `widget/CircularWidget.kt` — Glance는 `stringResource()`가 동작하지
  않아(별도 컴포지션 트리) 이미 다크모드 판별용으로 쓰던 `LocalContext.current`를 재사용해
  `context.getString(...)`으로 해석. `widget/NarrowWidget.kt`는 한국어 문자열이 전혀 없어 수정 불필요
- [x] `features/onboarding/OnboardingScreen.kt`, `features/settings/SettingsScreen.kt`,
  `features/stats/StatsScreen.kt`, `features/record/RecordSheet.kt`, `features/home/HomeScreen.kt`,
  `features/home/AchievementDialog.kt` — 나머지 모든 하드코딩된 한국어 UI 문자열을 `stringResource()`로 교체
- [x] 리소스 정리 중 발견한 부수 개선 — `HomeScreen.kt`의 `WaterEntryRow`가 `DrinkType`과 완전히
  중복되는 이모지/이름 when-분기를 직접 갖고 있던 것을 발견, `entry.drinkType.emoji`/
  `stringResource(entry.drinkType.displayNameRes)` 직접 참조로 교체(중복 제거)
- [x] `values/strings.xml` — 화면별 접두어로 그룹화한 신규 키 약 110개 추가 (기존 19개 유지),
  화면 간 동일 문구(현재 연속/최장 연속/뒤로/하루 목표/컵 크기/목표 감소·증가/연속 기록 보호 등)는
  공유 키로 통합해 중복 제거. `<string-array name="weekday_labels_short">` 신규(요일 한 글자 라벨)
- [x] `values-en/strings.xml` 신규 생성 — iOS `ko.lproj/Localizable.strings`에 이미 있는 영어 원문을
  그대로 재사용(Current Streak/Monthly/Yearly/Lifetime/47% off/7-Day Free Trial/Water/Coffee 등,
  CLAUDE.md의 플랫폼 간 UI 용어 일치 규칙 준수). iOS에 대응 원본이 없는 Achievement 6종 타이틀/메시지만
  새로 번역. 앱 내 언어 선택 UI는 추가하지 않음(iOS와 동일하게 시스템 언어만 따름).
  `privacy_policy_url`/`support_email`(translatable=false)은 영문 버전이 따로 없어 `values-en/`에서
  제외 — 기본 `values/` 값으로 자동 폴백
- [x] 기존 4개 테스트 파일(`HomeViewModelTest`/`BackupViewModelTest`/`StatsViewModelTest`) 생성자
  호출부에 Context mock 추가 — `HomeViewModelTest`는 `context.getString(...)` 정확한 인자까지 스텁해
  기존 스낵바 메시지 assertion을 그대로 유지, `StatsViewModelTest`는 `context.resources.getStringArray(...)`
  스텁으로 요일 배열 재현
- [x] 전체 111개 테스트 중 109개 통과 확인(`./gradlew testDebugUnitTest`), `./gradlew assembleDebug`
  빌드 성공 확인. 실패 2건은 v0.19 이전부터 있던 기존 버그(위 v0.19 섹션 참고)로 오늘 변경과 무관
- [x] **에뮬레이터 실측 검증 완료** — Pixel_8 에뮬레이터에서 시스템 언어를 English(United States)로
  전환 후 전 화면(온보딩 3페이지, 홈, 성취 다이얼로그, 음료 기록 시트, 통계, 설정 전 섹션, 프리미엄)
  스크린샷으로 확인. 이후 시스템 언어를 다시 한국어로 되돌려 기존 한국어 문구가 그대로 유지되는지도
  확인(회귀 없음)
  - `adb shell settings put system system_locales`만으로는 실제 Configuration이 갱신되지 않아
    (`dumpsys activity`로 `mCurrentConfig`가 안 바뀌는 것 확인 — 이 emulator 이미지는 non-rootable
    production build라 `am broadcast LOCALE_CHANGED`도 권한 거부됨), 시스템 설정 앱의 실제 언어
    변경 UI(`android.settings.LOCALE_SETTINGS` → 언어 추가 → 드래그로 우선순위 변경 → 확인 다이얼로그)를
    통해서만 언어 전환이 실제로 반영됨을 확인 — 향후 세션을 위한 참고 사항
  - **검증 중 발견 및 수정**: `OnboardingScreen.kt`의 목표 잔 수(`GoalPage`)와 `SettingsScreen.kt`의
    `DailyGoalSetting`이 각각 `Modifier.width(96.dp)`/`Modifier.width(80.dp)`로 고정폭이었는데, 한국어
    "8잔"은 짧아서 괜찮았지만 영어 "8 glasses"는 길어서 폭 안에서 단어 중간에 줄바꿈되어 잘리는 버그
    발견 → `widthIn(min = ...)` + `maxLines = 1`로 교체해 로케일에 따라 폭이 늘어나되 줄바꿈은 안 되게 수정

## v0.19 — 커스텀 Analytics 이벤트 계층 (리텐션/전환 KPI 측정)
- **배경**: v0.15에서 Firebase Analytics SDK만 연동되고 커스텀 이벤트가 전혀 없어 리텐션 세그먼트·프리미엄
  전환 퍼널을 측정할 수 없던 상태(남은 작업 P2 "제품 분석 도구" 항목)를 해소. D1/D7/D30 리텐션 자체는
  SDK가 자동 수집하는 세션/참여 이벤트로 충분하므로, 오늘 작업은 리텐션을 행동별로 세그먼트하고 무료→
  프리미엄 전환 퍼널(화면 조회 → 구매 시작 → 구매 성공)을 재는 커스텀 이벤트 + 유저 속성에 집중
- [x] `di/AnalyticsModule.kt` (신규) — `FirebaseAnalytics.getInstance(context)` Hilt 싱글턴 제공
- [x] `core/service/AnalyticsService.kt` (신규) — 이벤트명/파라미터명을 private const로 고정한 타입 있는
  로깅 메서드 노출 (범용 `logEvent(name, bundle)` 패스스루 금지, 오타 방지):
  `logScreenView`(표준 `screen_view`), `logRecordAdd`(`amount_ml`/`drink_type`/`source`),
  `logRecordUndo`, `logAchievementUnlocked`(`achievement_type`), `logOnboardingComplete`
  (`daily_goal`/`cup_size`/`notification_enabled`), `logPurchaseStarted`/`logPurchaseSuccess`/
  `logPurchaseCancelled`/`logPurchaseFailed`(`product_id`, 실패 시 `error_code`),
  `setPremiumUserProperty`(유저 속성 `is_premium`)
- [x] 계측 위치:
  - `navigation/WateringNavGraph.kt` — `LaunchedEffect` + `navController.currentBackStackEntryFlow`로
    라우트 변경마다 `screen_view` 1곳에서 중앙 로깅 (각 화면에 반복 코드 불필요).
    `MainActivity.kt`에서 `AnalyticsService`를 주입해 파라미터로 전달
  - `features/home/HomeViewModel.kt` — `addWater`/`addWaterCustom`에서 `record_add`(source="home"),
    `undoLastEntry`에서 `record_undo`
  - `widget/AddWaterAction.kt` — `record_add`(source="widget"), `widget/WateringWidgetState.kt`의
    `WidgetEntryPoint`에 `analyticsService()` 추가 (Glance `ActionCallback`은 생성자 주입 불가해
    EntryPointAccessors 경유)
  - `core/service/AchievementChecker.kt` — dedup을 통과해 실제로 신규 획득된 경우에만(`emit()` 내부)
    `achievement_unlocked` 기록
  - `features/onboarding/OnboardingViewModel.kt` — `completeOnboarding` 성공 후 `onboarding_complete`
  - `core/service/BillingService.kt` — `launchPurchase`에서 `purchase_started`(+ 취소/실패 콜백에서
    참조할 상품 ID를 `lastAttemptedProductId`에 임시 저장), `onPurchasesUpdated`의 OK/USER_CANCELED/
    else 3개 분기에서 각각 `purchase_success`/`purchase_cancelled`/`purchase_failed`, `queryPurchases()`
    에서 `_isPremium` 갱신 직후 `is_premium` 유저 속성 갱신(앱 시작 복원 체크·구매 후 재조회 양쪽 커버)
- **의도적으로 뺀 것**: `premium_view` 별도 이벤트(중앙화된 `screen_view(premium)`가 이미 커버),
  `widget_tap` 별도 이벤트(`record_add`의 `source` 파라미터로 대체), 구매 이벤트의 `value`/`currency`
  (오늘 요구사항은 매출 리포트가 아닌 전환 카운트라 범위 밖, 후속 작업으로 남김)
- [x] `AnalyticsServiceTest.kt`(신규, 13개) — `mockkConstructor(Bundle::class)`로 Android 프레임워크
  클래스(`Bundle`)를 가로채 이벤트명·파라미터를 검증 (Robolectric 미사용 프로젝트라 순수 MockK로 처리)
- [x] 기존 4개 테스트 파일(`AchievementCheckerTest`/`HomeViewModelTest`/`OnboardingViewModelTest`(2곳))
  생성자 호출부에 `analyticsService = mockk(relaxed = true)` 추가. `BillingServiceTest`는 확장 함수만
  테스트해 `BillingService`를 직접 생성하지 않으므로 변경 불필요
- [x] 전체 111개 테스트 중 109개 통과 확인(`./gradlew testDebugUnitTest`) — 실패 2건은 오늘 변경과 무관한
  기존 버그(`AchievementCheckerTest`의 `isAlreadyEarned` 특정 인자 스텁이 기본 스텁에 밀리는 문제로 추정,
  git stash로 v0.18 원본 코드에서도 동일하게 재현 확인. 아래 남은 작업에 별도 항목 추가)
- **알려진 갭(오늘 범위 밖, 수정 안 함)**: 위젯으로만 기록하는 유저는 `AddWaterAction`이
  `achievementChecker.check(...)`를 호출하지 않는 기존 버그 때문에 `achievement_unlocked`가 잡히지
  않음 (남은 작업에 별도 항목 추가)
- [x] **DebugView 실측 검증 완료** (Pixel_8 에뮬레이터, `adb shell setprop debug.firebase.analytics.app`
  + `adb logcat -s FA:V FA-SVC:V`) — 아래 6개 이벤트를 실제 조작으로 트리거해 파라미터까지 일치 확인:
  `screen_view`(home→stats→settings→premium→onboarding 라우트 전환마다 `ga_screen` 정확히 기록),
  `record_add`(source=home, amount_ml=200, drink_type=WATER), `record_undo`, `achievement_unlocked`
  (achievement_type=FIRST_SIP), `onboarding_complete`(daily_goal=8, cup_size=200,
  notification_enabled=1 — 앱 데이터 초기화 후 온보딩 재진행으로 검증)
  - **검증 중 발견**: 알림 권한 시스템 다이얼로그가 "시작하기" 버튼 위에 뜨는 타이밍에 탭하면 다이얼로그가
    탭을 가로채 버튼이 눌리지 않는 현상 발견(코드 버그 아님, 조작 실수였음 — 다이얼로그를 먼저 닫고
    재탭하니 정상 동작). 실제 사용자도 겪을 수 있는 시나리오라 UX 참고용으로 남김
  - **미검증(환경 제약)**: `record_add`(source=widget) — 위젯을 홈 화면에 배치하는 자동화가 이번 세션
    범위 밖이라 코드 리뷰로만 확인. `purchase_started`/`success`/`cancelled`/`failed`, `is_premium`
    유저 속성 — 이 에뮬레이터에서 `BillingService` 연결 자체가 실패함("Google Play In-app Billing API
    version is less than 3" — 기존에 남은 작업 P3에 문서화된 "에뮬레이터는 Play Billing 테스트 불가"와
    동일한 제약, 코드 문제 아님). 실기기/테스터 계정 확보 후 재검증 필요

## v0.18 — 백업 및 복원 기능 (Google 로그인 + Firestore)
- [x] `AuthService.kt` (신규, `core/service`) — Credential Manager(`androidx.credentials`) + `GoogleIdOption` 기반 Google 로그인. `FirebaseAuth.currentUser`를 `StateFlow`로 노출, `signIn(activityContext)`/`signOut()` 제공. 백업/복원 전용 선택적 로그인이며 온보딩과 무관, 로그인 없이도 앱은 완전히 동작
- [x] `BackupService.kt` (신규, `core/service`) — Firestore `backups/{uid}` 문서 1개에 스냅샷 저장/조회. `backup()`은 `todayRecord`/`streakInfo`/`history`/`settings`를 kotlinx.serialization JSON 문자열로 인코딩해 `set()`으로 항상 덮어쓰기, `restore()`는 문서를 읽어 `WaterRepository.restoreAll()` + `SettingsRepository.updateSettings()`로 로컬 데이터 전체 교체. 실시간 동기화가 아닌 수동 스냅샷 방식(기획 의도)
  - `Json { encodeDefaults = true }` — 필드 값이 데이터 클래스 기본값과 같아도 항상 명시적으로 인코딩. 생략 시 이후 앱 버전에서 기본값이 바뀌면 옛 백업을 복원할 때 다른 값으로 잘못 채워질 위험이 있어 안전하게 처리
- [x] `AuthModule.kt` (신규, `di`) — `FirebaseAuth.getInstance()` / `FirebaseFirestore.getInstance()` Hilt 싱글턴 제공
- [x] `BackupViewModel.kt` (신규, `features/settings`) — `BackupUiState`(Idle/Loading/Success/Error) 관리, 로그인 시 마지막 백업 시각 자동 조회, `signIn`/`signOut`/`backup`/`restore` 액션
- [x] `SettingsScreen.kt` — "백업 및 복원" 섹션 추가: 로그아웃 상태에서는 "구글로 로그인" 안내 카드, 로그인 상태에서는 계정 이메일 + 마지막 백업 시각 + 백업/복원/로그아웃 버튼
- [x] `WaterRepository`/`WaterDataStore` — `restoreAll(todayRecord, streakInfo, history)` 추가 (복원 시 세 값을 한 번에 덮어쓰기 위한 원자적 진입점)
- [x] `google-services.json` — Firebase 콘솔에서 Firestore 생성 + Google 로그인(Sign-in method) 활성화 후 재적용, OAuth 클라이언트(`client_type: 3`, 웹) 포함
- [x] 의존성 추가 — `firebase-auth`, `firebase-firestore`, `androidx.credentials`(-play-services-auth), `googleid`, `kotlinx-coroutines-play-services`
- [x] 단위 테스트 — `BackupServiceTest`(3개), `BackupViewModelTest`(11개) 작성. 전체 99개 테스트 전원 통과(`./gradlew testDebugUnitTest`)
  - 테스트 작성 중 발견/수정: `BackupViewModel`의 `init{}`이 `viewModelScope.launch{}`로 `getLastBackupTimestamp`를 즉시 호출하는데, 테스트의 `MainDispatcherRule`이 `UnconfinedTestDispatcher`라 ViewModel 생성 시점에 코루틴이 동기 실행됨 → mock 스텁을 ViewModel 생성 **이후**에 설정하던 기존 테스트 구조가 `MockKException`/조회 순서 불일치로 실패. mock을 `@Before`에서 미리 생성해두고 각 테스트가 "스텁 설정 → ViewModel 생성" 순서를 지키도록 재구성해 해결
- [x] `assembleDebug` 빌드 성공, 에뮬레이터(Pixel_8, API 35, Google Play 이미지)에 설치 후 설정 화면 UI 확인, "구글로 로그인" 버튼 동작 및 실패 시 에러 배너 표시 정상 확인
- **미검증 (후순위)**: 에뮬레이터에 등록된 Google 계정이 없어 로그인 성공 → 실제 백업/복원 왕복 플로우는 아직 실기기/계정 추가 후 검증 필요
- **후순위 체크 항목 유지**: Firebase 프로젝트 지원 이메일을 `wateringapp.help@gmail.com`으로 교체하는 작업은 보류 (2026-07-02 결정, 상세 내용 남은 작업 섹션 참고)

## v0.17 — 단위 테스트 확장 (ViewModel 레이어)
- [x] `turbine`(1.1.0) 추가 — `stateIn`/`combine` 기반 `StateFlow` 테스트용
- [x] `MainDispatcherRule`(신규, `testutil` 패키지) — `viewModelScope`가 쓰는 `Dispatchers.Main`을
  `UnconfinedTestDispatcher`로 교체하는 JUnit4 `TestWatcher`. 모든 ViewModel 테스트가 공유
- [x] `HomeViewModelTest` (9개) — 3개 Flow(`todayRecord`/`streakInfo`/`userSettings`) 조합 시 goal이
  설정값으로 덮어써지는지, `addWater`/`addWaterCustom`의 스낵바 메시지·업적 판정 연동,
  `dismissAchievement`가 STREAK_7 + activity 존재 조건에서만 리뷰를 요청하는지 검증
- [x] `SettingsViewModelTest` (7개) — `updateDailyGoal`만 위젯을 갱신하는지(다른 update*는 갱신 안 함),
  알림 on/off에 따른 예약/취소 분기. **JVM 단위 테스트 한계**: `Build.VERSION.SDK_INT`가 스텁 환경에서
  항상 0이라 `notificationPermissionGranted`는 항상 true 분기만 검증 가능 (거부 분기는 Robolectric/실기기
  영역, 테스트에 주석으로 명시)
- [x] `OnboardingViewModelTest` (5개) — 온보딩 완료 시 설정 저장 + 알림 활성화 여부에 따른 예약 분기
- [x] `PremiumViewModelTest` (5개) — `billingService`의 4개 StateFlow를 그대로 노출하는지, init 시
  `loadProducts()` 호출, purchase/restore/clearError 위임
- [x] `RecordViewModelTest` (10개) — 커스텀 입력 필터링(숫자만, 4자리 제한), 프리셋 선택 시 커스텀 모드
  해제, init 시 설정의 컵 크기 반영
- [x] `StatsViewModelTest` (5개) — 7일 통계 조합, 목표 달성일 집계, 주간 합계/평균 계산, 기록 없는 날
  0 처리
- **결과**: 6개 ViewModel 클래스, 총 41개 테스트 추가 (기존 43개 + 41개 = 전체 84개), 전원 통과
  (`./gradlew testDebugUnitTest`)
- **의도적으로 범위 밖에 둔 것**: `notificationPermissionGranted`의 권한 거부 분기(JVM 스텁 한계),
  Compose UI 레벨 테스트(Screen 컴포저블)는 별도 영역

## v0.16 — 단위 테스트 도입 (Service/Repository 레이어)
- [x] 테스트 의존성 추가 — `mockk`(1.13.13), `kotlinx-coroutines-test`(coroutines 버전과 동일하게 고정)
- [x] `WaterRepositoryTest` (12개) — `updateStreak()` 핵심 로직 전수 검증: 미달성/과거 기록 시 무변화,
  연속 기록 증가, 최장 기록 갱신, 당일 중복 호출 무변화, 프리미엄 연속 기록 보호(이틀 공백까지만
  보호, 3일 이상은 보호 대상 아님, 월 1회 제한), `resetToday`/`clearAllData` 위임 확인
- [x] `SettingsRepositoryTest` (5개) — DataStore 위임 확인
- [x] `WaterServiceTest` (5개) — `repository` 호출과 `widgetUpdater.updateAll()` 호출 순서까지 검증
  (위젯 갱신 순서 회귀 방지)
- [x] `AchievementCheckerTest` (10개) — 첫 잔/50% 돌파/목표 달성/연속 기록 타이틀(3·7·30일) 우선순위,
  이미 획득한 업적 재발급 방지 로직 전수 검증
- [x] `BillingServiceTest` (11개) — ISO 8601 기간 파싱(`P7D`/`P1M`/`P1Y`/`P1W`/복합 기간), 무료체험
  오퍼 우선 선택 로직. `parseIsoPeriodDays`를 `private`→`internal`로 변경(동작 변경 없음, 테스트
  접근용)
- **결과**: 5개 클래스, 총 43개 테스트 전원 통과 (`./gradlew test`)
- **의도적으로 범위 밖에 둔 것**: `ReviewService`(Play Core `ReviewManagerFactory` 등 프레임워크
  글루 코드 위주, 자체 로직 거의 없음), `NotificationService`/`MidnightResetWorker`/
  `NotificationWorker`(WorkManager 기반 — CLAUDE.md 기준 "통합 테스트" 영역이라 `TestListenableWorkerBuilder`
  필요, 단위 테스트와 별도 작업), DataStore 계층 자체(Repository 테스트에서 mock 처리로 대체,
  실제 DataStore I/O 검증은 별도 통합 테스트 영역)

## v0.15 — Firebase Crashlytics 연동
- [x] `google-services`/`firebase-crashlytics` Gradle 플러그인 추가 (루트 + `app` 모듈), `firebase-bom`
  기반 `firebase-crashlytics`/`firebase-analytics` 의존성 추가 (남은 작업 P2 "제품 분석 도구 없음"도
  같은 작업으로 함께 해결)
- [x] `app/google-services.json` 추가 — Firebase 프로젝트에 `com.watering.app`(release) +
  `com.watering.app.debug`(debug, `applicationIdSuffix` 대응) 2개 앱 등록 후 병합된 파일 사용.
  **git에 커밋 포함**(비밀키 아님, 공식 가이드상 커밋 안전) — 사용자 확인 후 결정
- [x] `app/src/debug/AndroidManifest.xml` (신규) — `firebase_crashlytics_collection_enabled=false`
  메타데이터로 디버그 빌드는 크래시 리포트 자동 전송 안 함 (release는 기본값 true로 자동 수집)
- [x] 에뮬레이터(Pixel_8) 설치 후 실행 검증 — 정상 기동, Crashlytics 내부 클래스 초기화 로그 확인,
  크래시/에러 없음
- [x] **AD_ID 권한 제거** — `firebase-analytics`가 매니페스트에 자동 병합한
  `com.google.android.gms.permission.AD_ID` / `ACCESS_ADSERVICES_AD_ID` 권한을 발견, 기획서
  13-2에 명시된 "광고 식별자 사용 안 함" 방침과 충돌하여 `tools:node="remove"`로 명시적 제거 +
  `google_analytics_adid_collection_enabled=false` 메타데이터로 이중 차단. 빌드 후 병합
  매니페스트에서 권한 제거 확인, 재설치 실행 검증(크래시 없음)
- [x] `기획서.md` 13-2 개인정보처리방침 핵심 내용 갱신 — "서버 전송 데이터: 없음"이 더 이상
  사실과 맞지 않아 Crashlytics/Analytics 수집 항목 명시하도록 수정
- [x] 실제 게시된 Google Sites 개인정보처리방침 페이지(iOS와 공유) 갱신 완료 — 2026-07-04,
  Crashlytics/Analytics/백업(Firestore)/Health Connect 데이터 흐름을 "1. 수집하는 정보" 항목에
  반영, "iOS 설정" 문구도 "기기 설정"으로 수정(안드로이드 사용자가 봐도 자연스럽게). 사용자가 직접
  게시 후 확인함
  - [ ] Play Console "Data Safety" 설문에 진단 데이터(충돌 로그)/앱 활동 데이터 신고 — 아직 미진행

## v0.14 — 프로젝트 정합성 점검 및 정리
- [x] **프리미엄 광고 정정**: `PremiumScreen.PREMIUM_FEATURES`에서 미구현 기능 4개(30일·연간 통계,
  수분 섭취율 분석, CSV 내보내기, 미세먼지·폭염 알림) 제거, "연속 기록 보호"만 노출 — 실제 결제
  화면에 없는 기능을 광고하던 상태였음(스토어 정책·환불 리스크). `SettingsScreen` 프리미엄 배너
  문구도 동일하게 수정. iOS `PremiumView`/`SettingsView`도 동시 수정, 미사용 번역 키 정리
  (`ko.lproj/Localizable.strings`)
- [x] `AndroidManifest.xml`: `allowBackup="false"` → `true`로 수정 — 기존엔 기기 변경/재설치 시
  물 기록·연속기록·설정이 전부 유실되는 상태였음. Android Auto Backup(기기 간 암호화 클라우드
  백업)이 이제 정상 동작
- [x] 서브에이전트로 문서-코드 정합성 전수 점검 완료 — 개발보고서 v0.10~v0.13 기재 내용과 실제
  코드 일치 확인, 죽은 코드/미사용 import 없음 확인. `기획서.md` 1장 플랫폼 표기("Android 13+")가
  실제 `minSdk=26`과 다른 점, 7장 상품 ID 예시 코드가 실제 ID와 다른 점 발견 (기획서 자체는 이번
  범위 밖 — 필요 시 별도 수정)
- **P3 백로그 갱신**: 제거된 4개 프리미엄 기능 + Health Connect + 대기질/폭염 알림을 "구현 후
  프리미엄 광고에 재추가" 항목으로 명시 (아래 P3 섹션 참고)
- **신규 발견 — 미착수**: Android 단위 테스트 0개(`app/src/test/` 디렉터리 자체가 없음, iOS는
  존재), Firebase Analytics 등 제품 분석 도구 부재, Android `values-en/` 영어 리소스 부재(iOS는
  존재) — 모두 P2 백로그에 추가

## v0.13 — 의견 보내기 (문의 + 개선 요청 통합)
- [x] `SettingsScreen` 앱 정보 섹션에 "의견 보내기" 버튼 추가 (`ACTION_SENDTO` mailto, 제목 "워터링 의견")
  - 최초 구현은 "문의하기"·"개선 요청하기" 2개 버튼으로 분리했으나, 사용자 피드백으로 **"의견 보내기" 1개 버튼으로 통합**
- [x] `strings.xml`에 `support_email` 리소스 추가 — 개발자 전용 이메일 `wateringapp.help@gmail.com` 확정 반영
- [x] iOS `SettingsView`/`SettingsViewModel`도 동일하게 "Send Feedback"(의견 보내기) 버튼 1개로 통합 + `mailtoURL`을 제목 파라미터 받는 함수로 변경, 기존 임시 주소(개인 Gmail)에서 신규 주소로 교체 — 두 플랫폼 동일 주소·동일 버튼 문구 사용
- 에뮬레이터로 인텐트 라우팅까지 확인(Gmail 앱 진입 확인) — 계정 미로그인 상태라 작성 화면까지는 못 봤으나 이는 환경 제약이지 코드 문제 아님

## 지원 범위 (Android 버전 / 기기)

| 항목 | 값 |
|---|---|
| minSdk | 26 (Android 8.0 Oreo, 2017) |
| targetSdk / compileSdk | 36 (Android 16) |
| 실질 커버리지 | 활성 기기의 99%+ (2026년 기준 Android 8.0 미만은 시장에서 사실상 소멸) |
| 기기 제약 | `AndroidManifest.xml`에 `<uses-feature>`/`<supports-screens>` 제약 없음 — Android 8.0+ 폰/태블릿 전체 설치 가능 (태블릿 UI는 v1 미최적화) |

> **(해결됨, 2026-07-02)** 이전엔 `docs/기획서.md` 1장이 "플랫폼: Android 13+ (API 33+)"로 기재돼 실제 `minSdk = 26`과 불일치했으나, 기획서 쪽을 정정 완료.

**버전별 기능 제약**:
- `POST_NOTIFICATIONS` 런타임 권한 요청은 Android 13(API 33)+ 에서만 발생 (그 미만은 항상 허용 상태로 동작)
- 잠금화면 위젯(`widgetCategory="keyguard"` 선언)은 Android 16 실기기(Galaxy A25)로 검증 완료 —
  다만 이 기기의 One UI 8은 서드파티 앱을 잠금화면 위젯 목록에 노출하지 않는 자사 앱 화이트리스트
  제약이 있음을 확인(v0.28.1 섹션 참고, 코드 문제 아님)
- Health Connect 연동 — v0.24~v0.26 구현 완료. 기기에 Health Connect 앱이 없으면 SDK 가용성 체크로
  기능이 조용히 비활성화됨(크래시 없음)

---

## 구현된 기능

### v0.12 — 인앱 리뷰 요청 + 위젯 다크모드 즉시 반영
- [x] `ReviewService` (신규) — Google Play In-App Review API(`review-ktx`) 연동. 7일 연속 달성(`STREAK_7`) 성취 다이얼로그를 닫는 순간 1회만 요청(감정적 최고점 타이밍, 기획서 9-3 리뷰 유도 전략 반영). `UserSettings.reviewRequested` 플래그로 재요청 방지
- [x] `HomeViewModel.dismissAchievement(activity)` — 닫힌 성취가 STREAK_7일 때만 `ReviewService` 호출하도록 연결. `HomeScreen`에 `findActivity()` 확장 추가
- [x] `WateringApp` — 위젯 다크모드 즉시 반영 버그 수정. 기존엔 시스템 다크모드를 켜도 위젯이 다음 데이터 변경(탭)까지 이전 테마로 고정되는 문제가 있었음
  - `registerComponentCallbacks(ComponentCallbacks2)`로 프로세스 생존 중 설정 변경 감지 → `widgetUpdater.updateAll()` 즉시 호출
  - 다크모드 전환은 백그라운드 프로세스를 종료시키는 경우가 많아 콜백만으로는 못 잡는 사례가 있어, SharedPreferences에 마지막 렌더링 시점의 다크모드 값을 남겨 **프로세스 재시작 시점(`onCreate()`)에도 비교 후 재갱신**하도록 이중 처리
  - 에뮬레이터에서 "앱 프로세스 종료 → 다크모드 전환 → 위젯 탭 없이 프로세스만 재시작" 시나리오로 검증 완료 (위젯 즉시 다크 테마 반영 확인)

### v0.11 — 알림 권한 요청 플로우 (Android 13+)
- [x] `SettingsViewModel` — `notificationPermissionGranted: StateFlow<Boolean>` 추가, `checkSelfPermission(POST_NOTIFICATIONS)` 기반 (API 33 미만은 항상 true). `refreshNotificationPermission()`으로 재확인 가능
- [x] `SettingsScreen` — `LifecycleEventEffect(ON_RESUME)`으로 화면 재진입 시(시스템 알림 설정 화면을 다녀온 뒤 포함) 권한 상태 자동 재확인
- [x] `NotificationPermissionBanner` (신규) — "물 마시기 알림"이 켜져 있는데 권한이 없으면 주황색 배너 표시: "알림 권한이 필요해요 / 설정 > 앱 > 알림에서 허용해주세요" + "설정 열기" 버튼 → `Settings.ACTION_APP_NOTIFICATION_SETTINGS`로 앱별 알림 설정 화면 직행 (API 26 미만은 `ACTION_APPLICATION_DETAILS_SETTINGS`로 폴백)
- iOS `SettingsView.notificationSection`의 배너+바로가기 패턴과 동일 UX (권한 재요청 다이얼로그 대신 시스템 설정으로 유도 — 두 플랫폼 동일)
- 온보딩 단계에서 이미 최초 1회 권한 요청은 되어 있었음(`OnboardingScreen`) — 이번 작업은 "거부 후 재진입" 케이스의 사각지대를 메움
- 에뮬레이터에서 `pm revoke`/`pm grant`로 권한 거부·허용 양쪽 시나리오 실제 검증 완료 (배너 표시/숨김, 설정 화면 딥링크 정상 동작)

### v0.10.3 — 위젯 아이콘·2×1 프로그레스 바 버그 수정
- [x] `ic_water_drop.xml` — 호(Arc) 명령 기반 경로가 Glance(RemoteViews) 렌더링에서 작은 크기(13~22dp)일 때 물방울이 아닌 작은 화살표(caret)로 깨져 보이던 문제 수정. 3차 베지어(C) 곡선만으로 구성된 경로로 교체 — 위젯 3종 모두에 공용 사용
- [x] `NarrowWidget.kt` — 2×1 위젯 프로그레스 바가 100% 달성 시에도 실제로는 짧게만 채워지던 버그 수정. 원인: `barTotal`을 `size.width - 고정값`으로 추정할 때 "N / M" 잔수 텍스트가 자릿수에 따라 폭이 달라지는데도 고정폭으로 가정 → 실측 위젯 폭(176dp)과 추정치가 어긋나 막대 트랙 자체가 지나치게 좁아짐. 잔수·퍼센트 텍스트에 명시적 고정 폭(`GlanceModifier.width()`)을 부여해 추정이 항상 정확하도록 수정, 막대에 더 많은 공간을 주기 위해 여백을 8/10/8dp→6/6/6dp로 축소(요청에 따라 막대 길이 확장)
- [x] `SettingsScreen.kt` — 알림 간격 옵션에 "4시간"(240분) 추가

### v0.10.2 — 구독/프리미엄 기능 코드 리뷰 및 최적화
- [x] `BillingService.loadProducts()` — 캐시 가드를 `isNotEmpty()` → `size >= 상품 3종`으로 수정. 기존 로직은 Play Console에 상품이 순차 등록되는 도중 일부만 조회 성공하면 그 불완전한 목록이 앱 프로세스 수명 내내 영구 캐싱되는 버그가 있었음
- [x] `BillingService.loadProducts()`/`queryPurchases()` — 구독/일회성 상품의 독립적인 두 네트워크 조회를 순차 `await` → `async`+`awaitAll` 병렬 실행으로 변경, 왕복 지연 절반으로 단축
- [x] `WaterRepository.updateStreak()` — `LocalDate.now()`를 함수당 4회 호출하던 것을 1회로 통합 (자정 경계에서 서로 다른 날짜를 참조할 수 있는 미세한 정합성 리스크 제거 + 불필요한 시스템 호출 감소)
- [x] `PremiumScreen.kt` — 배지(무료체험 일수)와 가격 문구가 각각 `trialOfferOrDefault()`를 중복 호출하던 것을 `pricing()` 단일 함수로 통합, `PlanCard`가 `ProductDetails`를 직접 몰라도 되도록 `priceLine: String?`만 받게 단순화
- iOS `StoreService.swift`에도 동일한 부분 캐시 버그 수정(`loadProducts()`) 및 `refreshStatus()` 조기 종료(entitlement 매칭 시 `break`) 적용 — 두 플랫폼 동시 반영
- [x] `BillingService.kt` — `trialOfferOrDefault()`/`freeTrialDays()` 확장 함수 추가. `launchPurchase()`에서 구독 오퍼 중 무료체험(가격 0원 단계 포함) 오퍼를 우선 선택하도록 변경 (Play가 가입 이력상 자격 없는 유저에게는 애초에 트라이얼 오퍼를 내려주지 않으므로 별도 자격 검증 불필요)
- [x] `PremiumScreen.kt` — 월간 플랜에 무료체험 오퍼가 있으면 "7일 무료체험" 배지 + "7일 무료체험 후 ₩3,900 / 월" 문구 자동 표시 (`priceLine()` 신규)
- iOS `Products.storekit`에는 이미 monthly 상품에 7일 무료체험(FREE_TRIAL, 1 WEEK)이 설정되어 있어 동일 UX로 동시 구현
- **주의**: Play Console에 monthly 상품의 실제 오퍼(7일 무료체험)가 등록되어야 실기기에서 배지가 표시됨. 미등록 시 일반 오퍼로 폴백되어 배지 없이 정가만 표시

### v0.10 — Google Play Billing 구독 구현 (iOS StoreKit 2 구현 대응)
- [x] `BillingService.kt` (신규) — `BillingClient` 싱글턴(Hilt), 상품 3종(월/연 구독 + 평생 일회성) 조회, 구매 플로우, `queryPurchasesAsync` 기반 구독 상태 갱신, `PurchasesUpdatedListener`로 구매 결과 반영, `acknowledgePurchase` 처리. 서버 없이 클라이언트 조회만으로 검증 (iOS `Transaction.currentEntitlements` 방식과 동일)
- [x] `WateringApp` — `onCreate()`에서 `billingService.startConnection()` 자동 호출 (iOS `refreshStatus()` 자동 호출과 동일)
- [x] `PremiumScreen.kt` 전면 재작성 — 기능 5종 안내(연속 기록 보호, 30일/연간 통계, 수분 섭취율 분석, CSV 내보내기, 미세먼지·폭염 알림 연동), 플랜 카드 3종(월/연/평생) 선택 UI, 구매·복원 버튼, 이미 구독 중일 때 별도 화면, 하단 결제 안내 문구
- [x] `PremiumViewModel.kt` (신규) — `BillingService` 상태를 Compose에 노출
- [x] `SettingsScreen.kt` — 최상단에 프리미엄 섹션 추가 (미구독 시 업그레이드 카드 → PremiumScreen 이동, 구독 중이면 "프리미엄 이용 중" 배지) — iOS `premiumSection` 배치와 동일
- [x] `WateringNavGraph.kt` — Settings → Premium 네비게이션 연결
- [x] 연속 기록 보호 로직 실제 구현 — `WaterRepository.updateStreak()`에 `isPremium` 파라미터 추가, 정확히 하루를 놓친 경우에만 월 1회 보호 적용(`protectionUsedThisMonth`/`protectionUsedMonthKey` 필드 실사용). `WaterService`, `HomeViewModel`, `AddWaterAction`(위젯) 호출부 모두 갱신
- 상품 ID: `com.watering.app.premium.monthly`(월 ₩3,900) / `com.watering.app.premium.yearly`(연 ₩24,900, 구독) / `com.watering.app.premium.lifetime`(₩49,900, 일회성 in-app product — iOS와 달리 Play Billing은 평생 상품을 구독이 아닌 별도 타입으로 등록해야 함)
- **주의**: Play Console에 위 3개 상품이 실제 등록되어야 `loadProducts()`가 정상 동작함 (미등록 시 상품 목록 빈 배열)

### v0.9.4 — 위젯 연속 탭 시 화면 고정 버그 수정 (에뮬레이터 실기기 테스트로 발견)
- [x] `WateringWidgetState.kt` — `rememberWidgetState()` 신규: `currentState(WidgetRefreshKey)`를 키로 하는 `produceState`로 위젯 상태를 다시 읽도록 변경
- [x] `CircularWidget`/`RectangularWidget`/`NarrowWidget` — `provideGlance()`에서 1회성으로 캡처하던 `state`를 제거하고 `rememberWidgetState(context)`로 교체
- [x] `WateringWidgetUpdater` — `updateAll()`에서 위젯 갱신 직전 `updateAppWidgetState()`로 refresh 토큰을 갱신해 Glance 세션 재사용 시에도 최신 데이터를 다시 읽도록 함
- 원인: Glance의 `GlanceAppWidget.provideGlance()`는 세션(같은 위젯 인스턴스)이 살아있는 동안 최초 1회만 호출된다. 빠르게 연속으로 위젯을 탭하면 이전 세션이 재사용되면서 `update()`가 여러 번 호출돼도 최초 호출 시점의 데이터로 화면이 고정되는 현상이 있었음(데이터 자체는 항상 정확했음). 에뮬레이터에서 위젯을 5연타 → 로그상 데이터는 정상 증가하지만 화면은 고정되는 것을 확인 후 수정, 재검증 완료(고정 없이 즉시 반영됨).

### v0.9.3 — 코드 리뷰 발견 버그 수정
- [x] `WaterDataStore` — `addEntry`/`removeLastEntry` 신규: DataStore `edit{}` 트랜잭션 내부에서 최신 상태를 읽어 갱신하는 원자적 처리로 변경 (연속 탭 시 기록 유실 가능성 있던 race condition 수정)
- [x] `WaterRepository`/`WaterService`/`HomeViewModel`/`AddWaterAction` — stale snapshot(`currentRecord`) 전달 방식 제거, `goal` 값만 전달하도록 시그니처 정리 (위젯 퀵 추가 버튼도 동일 버그 있어 함께 수정)
- [x] `SettingsViewModel` — `dailyGoal` 변경 시에만 `widgetUpdater.updateAll()` 호출하도록 수정 (알림 설정 등 위젯과 무관한 변경 시 불필요한 갱신 제거)
- [x] `HomeScreen` — "마지막 취소" 텍스트에 `clickable` + `Role.Button` 시맨틱 추가 (클릭 핸들러가 없어 동작하지 않던 죽은 UI 수정)
- [x] `WaterDataStore`/`SettingsDataStore` — `editSafely()` 헬퍼 추가, 모든 DataStore 쓰기에 에러 로깅 + 예외 전파 적용
- [x] `AchievementChecker` — 주석 내 금지 용어 "스트릭" → "연속 기록"으로 수정

### v0.9.2 — 앱 아이콘 Adaptive Icon 재구현

- [x] `mipmap-anydpi-v26/ic_launcher.xml` — Adaptive Icon 방식으로 재구현
  - Background: `@drawable/icon_transparent` (투명 — 배경 링 제거)
  - Foreground: `@drawable/watering_icon3_fg` (비네트 처리된 소스 이미지)
- [x] `watering_icon3_fg.png` (신규) — 소스 이미지 외곽 15% 구간(85~100% 반경) 비네트 적용(최대 70% 어둡게), 투명 코너 유지
- [x] `icon_transparent.xml` (신규) — 투명 배경 shape drawable
- [x] `watering_icon3_bg.png` (신규) — 비네트 처리 + 불투명 합성 버전 (밀도별 mipmap ic_launcher.png 생성용)
- [x] `mipmap-{mdpi|hdpi|xhdpi|xxhdpi|xxxhdpi}/ic_launcher.png` — 비네트 처리 버전으로 교체
- [x] `mipmap-{mdpi|hdpi|xhdpi|xxhdpi|xxxhdpi}/ic_launcher_round.png` — 비네트 처리 버전으로 교체

### v0.9 — 코드 최적화 & 정리
- [x] `WateringWidgetUpdater` — `Dispatchers.Main` → `Dispatchers.Default` (Glance는 Main thread 불필요)
- [x] `themes.xml` — Dead Code `Theme.Watering.Splash` 스타일 제거 (시스템 SplashScreen API 미사용)
- [x] `WidgetEntryPoint` — 미사용 `widgetUpdater()` 메서드 제거 (WaterService 내부에서 자동 호출)
- [x] `WateringWidgetUpdater` + `AddWaterAction` — 위젯 갱신 시 logcat 디버그 로그 추가
- [x] 앱 아이콘 — 사용자 제공 이미지를 mipmap-mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi 전 밀도 배포, adaptive icon XML 제거
- [x] `MainActivity` — `installSplashScreen()` 제거, Activity 테마 `Theme.Watering`으로 통일

### v0.8 — 스플래시 화면 + 앱 아이콘 리디자인
- [x] `SplashScreen.kt` (신규) — 브랜드 이미지(`splash_image.png`) 전체화면 표시, 1.5초 후 자동 전환
- [x] `WateringNavGraph.kt` — `Screen.Splash` 추가, startDestination을 `splash`로 변경, splash → onboarding/home 분기
- [x] `ic_launcher_icon.png` 추가 — 사용자 제공 아이콘 이미지 drawable에 배치
- [x] `AddWaterAction` — 위젯 탭 후 즉시 갱신: `WidgetEntryPoint`에 `widgetUpdater()` 노출 → 현재 WaterService 내부에서 통합 처리로 변경

### v0.7 — 위젯 다크/라이트 모드 자동 대응
- [x] CircularWidget — OS `Configuration.UI_MODE_NIGHT_MASK` 감지, 라이트 시 흰 배경 + 아쿠아 텍스트
- [x] RectangularWidget — 다크/라이트 분기 (배경·텍스트·진행 바 트랙 색상)
- [x] NarrowWidget — 다크/라이트 분기
- [x] SettingsViewModel — `widgetUpdater.updateAll()` 추가 (설정 변경 즉시 위젯 갱신)

### v0.6 — 위젯 크기 축소
- [x] CircularWidget: 2×2 → **1×1** (`targetCellWidth/Height=1`, `minWidth/Height=40dp`)
- [x] NarrowWidget: 4×1 → **2×1** (`targetCellWidth=2`, `minWidth=110dp`)
- [x] RectangularWidget: 4×2 → **2×2** (`targetCellWidth/Height=2`, `minWidth/Height=110dp`)

### v0.5 — 성취 타이틀 시스템 & 위젯 디자인 리뉴얼
- [x] `Achievement.kt` — 6종 타이틀 enum (FIRST_SIP / HALF_WAY / GOAL_ACHIEVED / STREAK_3/7/30)
- [x] `AchievementDataStore.kt` — 오늘 날짜 기준 중복 방지 (`DataStore.first()` 패턴)
- [x] `AchievementChecker.kt` — addWater 전후 DayRecord 비교로 달성 조건 검사, 스트릭 타이틀 우선 반환
- [x] `AchievementDialog.kt` — 풀스크린 Dialog (`usePlatformDefaultWidth=false`), scale-in + 파티클(✦) 애니메이션
- [x] HomeViewModel — `pendingAchievement: StateFlow<Achievement?>`, `dismissAchievement()` 추가
- [x] HomeScreen — `pendingAchievement` 관찰 → `AchievementDialog` 표시
- [x] `WateringWidgetState.kt` — `SettingsRepository` 연결, `settings.dailyGoal` 반영
- [x] HomeViewModel — `record.copy(goal = settings.dailyGoal)` goal sync 수정
- [x] CircularWidget 리뉴얼 — 아쿠아/그린 배경, 물방울 아이콘, 큰 숫자, 하단 진행 바
- [x] RectangularWidget 리뉴얼 — 다크 배경, 타이틀 행 + 숫자 + % + 진행 바 + 동기부여 문구
- [x] NarrowWidget 리뉴얼 — 다크 배경, 아이콘 + 잔수 + 가로 진행 바 + %

### v0.4 — 자정 초기화 WorkManager + 앱 아이콘
- [x] MidnightResetWorker — 매일 자정 DayRecord 초기화, 완료 후 다음날 자정 재등록
- [x] NotificationService.scheduleMidnightReset() — 다음 자정까지 delay 계산 후 OneTimeWorkRequest 등록
- [x] WateringApp — HiltWorkerFactory 설정 + Configuration.Provider 구현 (NotificationWorker/MidnightResetWorker 동작 전제)
- [x] AndroidManifest — WorkManager 기본 자동 초기화 비활성화 (HiltWorkerFactory 커스텀 사용)
- [x] 앱 아이콘 — adaptive icon (foreground 물방울/background #007AFF) + monochrome (API 33+)

### v0.3 — 설정 화면 (SettingsScreen)
- [x] SettingsViewModel — `StateFlow<UserSettings>`, 항목별 update 메서드, 알림 재스케줄링 연동
- [x] SettingsScreen — 목표 잔 수(슬라이더), 기본 컵 크기(FilterChip), 알림 on/off(Switch), 알림 간격(FilterChip), 알림 시작/종료(시간 조정 버튼), Health Connect(Switch)
- [x] 알림 서브설정 AnimatedVisibility — 알림 꺼짐 시 간격·시간 설정 숨김 처리

### v0.2 — 음료 기록 바텀시트 (RecordSheet)
- [x] DrinkType에 `emoji`, `displayName` computed property 추가
- [x] RecordViewModel — 음료 타입 / 용량 선택, 직접 입력, 컵 사이즈 초기값 로드
- [x] RecordSheet — ModalBottomSheet, DrinkTypeSelector(FilterChip FlowRow), AmountSelector(프리셋+직접입력)
- [x] HomeViewModel.addWaterCustom(amount, drinkType) 추가
- [x] HomeScreen — "음료 종류 선택 +" OutlinedButton, RecordSheet 연결

### v0.1 — 코어 인프라 & 홈 화면
- [x] Hilt DI 모듈 설정 (DataModule, ServiceModule)
- [x] DataStore 레이어 (WaterDataStore, SettingsDataStore)
- [x] Repository 레이어 (WaterRepository, SettingsRepository)
- [x] WaterService — 기록 추가, undo, streak 계산
- [x] 데이터 모델 (DayRecord, WaterEntry, DrinkType, UserSettings, StreakInfo)
- [x] HomeScreen — 진행률 링, 1탭 기록 버튼, 스트릭 카드, 오늘 기록 타임라인, Undo 스낵바
- [x] HomeViewModel — StateFlow UiState, addWater, undoLastEntry
- [x] Navigation (WateringNavGraph) — Home / Stats / Settings / Premium / Onboarding
- [x] Glance 위젯 3종 (CircularWidget, RectangularWidget, NarrowWidget) 초기 구조
- [x] WorkManager 알림 스케줄링 (NotificationWorker)

---

## 파일 구조

패키지 단위 구성만 요약(개별 파일이 계속 추가/이동돼 exhaustive한 트리는 금방 낡음 — 실제
목록은 코드/Glob으로 직접 확인할 것):

- `features/{home,record,stats,settings,onboarding,premium,splash}/` — 화면별 Screen + ViewModel
- `core/model/` — 데이터 모델(`DayRecord`, `WaterEntry`, `DrinkType`, `UserSettings`, `StreakInfo`,
  `Achievement`, `DailyAchievement`, `WidgetTheme` 등)
- `core/data/` — Repository, `core/datastore/` — DataStore/AchievementDataStore
- `core/service/` — 비즈니스 로직/외부 연동 (`WaterService`, `NotificationService`,
  `HealthConnectService`, `BillingService`, `AuthService`, `BackupService`, `AnalyticsService`,
  `CsvExportService`, `AchievementChecker`, `StatsInsightService` 등)
- `widget/` — Glance 위젯 3종(`CircularWidget`/`RectangularWidget`/`NarrowWidget`) + 공용 상태/갱신 로직
- `di/` — Hilt 모듈, `ui/theme/` — 테마/타이포그래피
- 각 파일이 정확히 어느 버전에 추가/변경됐는지는 위의 버전별 섹션(v0.1~) 참고 — 여기서 중복 유지하지 않음
```

---

## 남은 작업

### P1 — 출시 필수
- [x] SettingsScreen 구현 (목표 잔 수, 컵 사이즈, 알림 시간 설정)
- [x] 자정 초기화 WorkManager 작업 (OneTimeWorkRequest, 다음 자정까지 딜레이)
- [x] Glance 위젯 3종 리뉴얼 + 실기기 데이터 연동 확인
- [x] 위젯 크기 최적화 (1×1 / 2×1 / 2×2)
- [x] 위젯 다크/라이트 모드 자동 대응
- [x] 설정 변경 시 위젯 즉시 갱신
- [x] 성취 타이틀 시스템 (6종 타이틀, 풀스크린 애니메이션 모달)
- [x] OnboardingScreen 구현
- [x] 앱 아이콘 (사용자 이미지 전 mipmap 밀도 배포)
- [x] 스플래시 화면 (브랜드 이미지 전체화면, 시스템 스플래시 제거)
- [x] 위젯 탭 후 숫자 즉시 갱신 검증 (에뮬레이터 실기기 테스트 완료 — 단일 탭 정상, 연속 탭 시 화면 고정 버그 발견 및 수정)
- [x] AndroidManifest 권한 목록 최종 검토 — SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM 제거 (WorkManager 미사용, Play 정책 위반 방지)
- [x] 개인정보처리방침 URL — SettingsScreen 앱 정보 섹션에 링크 추가 (sites.google.com/view/watering-privacy)
- [x] 에뮬레이터에서 다른 위젯 크기(2×1 좁은, 2×2 직사각형) 탭 갱신 검증 완료 — Pixel_8 에뮬레이터 홈 화면에 1×1/2×1/2×2 3종 모두 배치 후 2×1·2×2 각각 5연타 테스트. `adb logcat -s WateringWidget` 기준 `AddWaterAction triggered` → 3개 위젯 모두 `updating` → `after: N/8` 순서로 매 탭마다 정상 처리(1/8→5/8, 6/8→10/8), 화면 고정 없이 즉시 반영. v0.9.4 수정 사항이 2×1/2×2에도 동일하게 적용됨을 확인

### P2 — 품질 향상
- [x] StatsScreen — 7일 바 차트 (Compose Box 기반 커스텀 바 차트, 목표선 + 색상 분기 구현)
- [x] 알림 권한 요청 플로우 (Android 13+) — v0.11, 배너+설정 딥링크
- [x] 인앱 리뷰 요청 플로우 — v0.12, STREAK_7 달성 시 1회 요청
- [x] 위젯 다크모드 즉시 반영 — v0.12, 프로세스 생존/재시작 양쪽 케이스 처리
- [x] 위젯→앱 딥링크 (quickRecord=true) 검증 — v0.28.5 완료. 2026-07-04 정적 코드 분석으로 위젯
  3종이 전부 `actionRunCallback<AddWaterAction>()`(백그라운드 즉시 기록)만 쓰고 이 딥링크를 호출하는
  곳이 프로젝트 전체에 없음을 확인, 사용자 결정에 따라 미사용 코드 전체 제거(자세한 내용은 v0.28.5
  섹션 참고)
- [x] Google Play 스토어 등록 자료 1차 준비 — `docs/store/` (아이콘 512, 스크린샷 6장, 전체/짧은 설명문, 키워드) — 아래 "Play 스토어 등록 남은 작업" 참고
- [x] 개발자 연락 이메일 확보 — `wateringapp.help@gmail.com`
- [x] 앱 내 "의견 보내기" 기능 — v0.13, SettingsScreen "앱 정보" 섹션에 mailto 버튼 (iOS 동시 반영)
- [x] 실기기에서 잠금화면 위젯 동작 검증 — Galaxy A25(Android 16)로 최초 실기기 테스트 완료
  (v0.28.1 섹션 참고). `initialLayout` 누락 버그는 발견·수정했으나, 잠금화면 목록 미노출 자체는
  One UI가 자사 앱만 화이트리스트로 노출하는 **플랫폼 제약**으로 결론(코드로 해결 불가) — 다른
  제조사 기기 확보 또는 삼성 업데이트 대기 필요
- [x] **Firebase Crashlytics 연동** — v0.15 완료. `google-services.json` 배치, Gradle 플러그인 추가, 디버그 빌드 자동 수집 비활성화까지 반영. Analytics도 같은 작업으로 함께 추가됨
  - [x] 개인정보처리방침에 크래시 리포트/Analytics 데이터 수집 명시 — 2026-07-04 완료. 백업
    (Firestore)/Health Connect 데이터 흐름도 함께 반영해 실제 게시본 갱신
- [x] **백업 및 복원 기능** — v0.18 완료. Google 로그인 + Firestore 기반 수동 백업/복원. 코드·Firebase 콘솔 설정(Firestore 생성, Google 로그인 활성화, 보안 규칙 게시) 완료, 빌드·단위 테스트·에뮬레이터 UI 검증 완료
  - [ ] 실제 Google 계정으로 로그인 → 백업 → 복원 왕복 플로우 실기기/계정 추가 검증 — 2026-07-04
    시도 중 중단: adb로 설정 화면까지는 자동 이동 확인(오늘 기록 7/5잔 기준점, "구글로 로그인" 버튼
    위치 확인)했으나, Credential Manager 계정 선택 화면은 자동 승인 정책(민감한 인증 화면 블라인드
    탭 차단)에 막혀 사용자가 직접 계정을 선택해야 함. 로그인 이후 백업/기록추가/복원 검증은 다음
    세션에서 이어서 진행 예정
  - [ ] **후순위 체크 항목**: Firebase 프로젝트의 "지원 이메일"을 현재 로그인 계정 대신 `wateringapp.help@gmail.com`으로 변경 필요 — 해당 이메일을 프로젝트 멤버로 초대한 뒤 Authentication → Sign-in method → Google 설정에서 지원 이메일을 교체해야 함 (2026-07-02, 사용자가 임시로 본인 계정으로 진행하기로 결정)
- [x] **단위 테스트 도입** — v0.16~v0.17 완료. Service/Repository 5개 클래스(43개) + ViewModel
  6개 클래스(41개), 총 84개 테스트 (자세한 내용은 v0.16/v0.17 섹션 참고)
  - **2026-07-02, 사용자 결정으로 후순위 전환** — 아래 항목은 지금 단계에서 진행하지 않고 보류:
  - [ ] `ReviewService`/`NotificationService`/`MidnightResetWorker`/`NotificationWorker` — WorkManager·Play
    Core 연동 부분, 통합 테스트로 별도 진행 필요 (후순위)
  - [ ] Service 레이어 브랜치 커버리지 80%+ 목표 대비 실측 미확인 — 커버리지 도구(Jacoco 등) 미설정 (후순위)
  - [ ] Compose Screen(UI) 레벨 테스트 — 아직 미착수 (후순위)
- [x] **영문버전 안드로이드 앱** — v0.20 완료. 하드코딩된 한국어 문자열 약 110개를 `strings.xml`로
  추출 + `values-en/strings.xml` 신규 생성 + 에뮬레이터 전 화면 실측 검증 완료(영어→한국어 회귀 없음
  확인, 고정폭 텍스트 줄바꿈 버그 1건 발견·수정). 자세한 내용은 v0.20 섹션 참고
- [x] **제품 분석 도구 (사용자 행태 추적)** — v0.19 완료. `AnalyticsService` + 커스텀 이벤트(record_add/undo,
  achievement_unlocked, onboarding_complete, 구매 퍼널 4종) + `is_premium` 유저 속성, `screen_view`
  중앙 로깅. 자세한 내용은 v0.19 섹션 참고
  - [x] Firebase DebugView 실측 검증 완료 — screen_view/record_add(home)/record_undo/
    achievement_unlocked/onboarding_complete 6종 파라미터까지 확인 (자세한 내용 v0.19 섹션 참고)
  - [ ] `record_add`(source=widget), 구매 퍼널 4종, `is_premium` 실측 검증 — 위젯 미배치·에뮬레이터
    Billing API 미지원으로 이번엔 코드 리뷰만, 실기기 확보 후 재검증 필요
  - [ ] 구매 이벤트에 `value`/`currency` 파라미터 추가(매출 리포트용) — 오늘은 전환 카운트만 범위, 후속 작업
  - [x] 위젯으로만 기록하는 유저는 `achievement_unlocked`가 안 잡히는 기존 갭 — v0.28.2 완료
    (자세한 내용은 v0.28.2 섹션 참고)
  - [x] 위젯 기록 시 성취 모달을 앱 재진입 시 보여주는 것 — v0.29.7 완료(`AchievementDataStore`
    pending 상태 저장 + `HomeViewModel.init`에서 로드). 단위 테스트로 로직 검증, 실사용 중 실제
    노출 여부는 실기기 터치 입력 불안정으로 다음 세션에서 재확인 필요(자세한 내용은 v0.29.7 섹션 참고)
- [ ] 목표 자동 계산 기능 없음 — 경쟁 앱(WaterMinder 등)은 체중·활동량 기반 목표 추천, 워터링은 사용자가 숫자 직접 선택만 가능 (선택 사항, 우선순위 낮음)
- [~] **디자인/UI 개선 — 2026-07-04 사용자 결정으로 후순위 전환**: 홈 화면 파스텔 팔레트(v0.28.4)와
  물방울/불/트로피 벡터 아이콘 전환까지는 완료. 아래는 미결정 상태로 보류:
  - [ ] 음료별(커피/차/주스/우유/기타) 아이콘 스타일 — Material Symbols/Tabler Icons 후보 모두
    마음에 안 든다고 판단, 다른 방향 필요
  - [ ] 위젯 쪽 디자인(색상/레이아웃/아이콘) 파스텔 팔레트 반영 — 앱 쪽만 반영했고 위젯 3종은
    아직 기존 아쿠아(`WidgetTheme` 등) 그대로
  - [ ] 프리미엄 배너·위젯 테마 변경 옵션의 화면 내 위치 — "유료 추천이 UI 사용을 방해하면 안 됨"
    이라는 원칙만 확인, 구체적 배치는 미정
  - [x] 앱 아이콘 물방울이 확대돼 보이는 문제 — v0.29.6 완료. 세이프존 비율(약 61%)에 맞춰
    `watering_icon3_fg.png`를 0.62배로 축소·재배치, 실기기 확인 완료(자세한 내용은 v0.29.6 섹션 참고)
- [x] **테스트 버그**: `AchievementCheckerTest`의 `check_이미획득한업적은다시반환하지않는다`/
  `check_streak타이틀이미획득했으면_GOAL_ACHIEVED로대체된다` 2건 실패 — 2026-07-03 같은 날 적용된
  v0.24.1 수정(`AchievementChecker`가 `LocalDate.now()` 대신 `DayRecord.dateKey`를 쓰도록 변경)이
  근본 원인을 함께 해결한 것으로 확인(v0.24.1 커밋 로그에 "전체 단위 테스트 135개 전원 통과" 명시).
  2026-07-04 재검증 — 해당 테스트 2건 포함 `AchievementCheckerTest` 19개 전체 3회 반복 실행에서도
  안정적으로 통과, 플래키 아님 확인. 이 체크리스트 항목만 갱신되지 않고 남아있던 상태였음

### Play 스토어 등록 남은 작업
- [x] 스크린샷 6장 (`docs/store/screenshots/01~06`) — 홈/음료기록시트/위젯3종/통계/설정/다크모드
- [ ] 잠금화면 위젯 스크린샷 — 실기기 검증 후 추가 촬영 필요
- [x] 앱 아이콘 512×512 (`docs/store/play_store_icon_512.png`)
- [ ] **피처 그래픽 1024×500 — 디자이너 작업 보류 상태** (사용자 확인 필요, 현재는 스킵 결정)
- [x] 짧은 설명 / 전체 설명 / 키워드 (`docs/store/listing_ko.md`)
- [ ] 콘텐츠 등급 설문 (Play Console에서 직접 진행)
- [x] 개발자 연락 이메일 — `wateringapp.help@gmail.com`
- [x] 개인정보처리방침 URL (기존 sites.google.com/view/watering-privacy 재사용)

### P3 — 프리미엄 (2026-07-05, 사용자 결정으로 전면 보류 — 전 기능 무료 전환, v0.30.0 섹션 참고)
- [x] Google Play Billing 구독 구매 플로우 (v0.10 — BillingService/PremiumScreen/연속 기록 보호)
- [~] Play Console 상품 3종 실등록 (monthly/yearly/lifetime) — 프리미엄 자체를 없애기로 해서 더 이상 필요 없음, 코드(BillingService/PremiumScreen)는 휴면 상태로 보존만 함
- [~] 실기기/테스터 계정으로 구매·복원 플로우 실제 검증 — 위와 같은 이유로 보류

**2026-07-03 프리미엄 기능 재기획**: 기존엔 "연속 기록 보호권" 단일 기능뿐이었는데, 월 1회 이하로만
체감돼 구독 유지 동기가 약하다고 판단해 재기획. 경쟁앱(WaterMinder/Waterllama/Hydro Coach/Plant
Nanny) 비교 + 구독 이탈 방지 리서치 근거로 "매일 체감되는 가치" 위주로 재구성(상세 근거는
`docs/기획서.md` 8-2 및 세션 대화 기록 참고). 아래는 새 우선순위 — 구현 완료된 것만 순서대로
`PremiumScreen.PREMIUM_FEATURES`(Android)/`PremiumView.features`(iOS) 양쪽에 추가할 것
(미구현 상태로 광고 금지 원칙 유지):
- [x] **1순위 — 위젯 프리미엄 테마** — v0.21 완료. 4종(기본/선셋/모노/네온) 구현 + 에뮬레이터 실측
  검증까지 완료. 자세한 내용은 v0.21 섹션 참고
- [x] **2순위 — 스마트 통계** — v0.22 완료. 30일 트렌드 + 시간대별 패턴 인사이트 + 연간 히트맵
  구현 + 에뮬레이터 실측 검증까지 완료. 자세한 내용은 v0.22 섹션 참고
- [~] **3순위 — 스마트 목표 추천 + 음료별 수분 환산율** (4단계로 분리 진행 중):
  - [x] Phase 1 — 음료별 수분 환산율 정보성 지표: v0.23 완료(자세한 내용은 v0.23 섹션 참고)
  - [x] Phase 2 — Health Connect 인프라 구축(의존성/권한/`HealthConnectService`): v0.24 완료(자세한
    내용은 v0.24 섹션 참고)
  - [x] Phase 3 — 체중 읽기 → 스마트 목표 추천(프리미엄, 다이어트 프레이밍 배제): v0.25 완료
    (자세한 내용은 v0.25 섹션 참고). **2026-07-05, 사용자 결정으로 프리미엄에서 제외해 무료 전환** —
    Health Connect 자동 읽기 대신 몸무게 직접 입력 방식으로 변경(v0.29.9 섹션 참고)
  - [x] Phase 4 — 물 마시기 기록 → Health Connect HydrationRecord 자동 저장(무료): v0.26 완료
    (자세한 내용은 v0.26 섹션 참고) — **3순위 4단계 전체 완료**
- [x] **4순위 — 연속 기록 보호권 마일스톤 확장**: 기존 보호권(월 1회)은 유지, 60/100/365일
  프리미엄 전용 마일스톤 타이틀 추가 — v0.27 완료(자세한 내용은 v0.27 섹션 참고). 에뮬레이터
  실측은 실제 60일 이상 소요되어 후순위(단위 테스트로 로직 검증 완료)
- [x] **5순위 — 기록 CSV 내보내기**: v0.28 완료(자세한 내용은 v0.28 섹션 참고) — **프리미엄 재기획
  5개 항목 전체 완료**
- [ ] Health Connect 연동 자체는 무료 유지(기획서 원안대로 Play "건강" 카테고리 노출 목적) — 3순위의
  선행 조건으로만 필요
- [ ] 대기질/폭염 알림 연동 (기상청/에어코리아 API) — 재기획에서 우선순위 밖으로 밀림, 필요시 향후 재검토
- **의도적으로 제외한 방향**: Waterllama식 캐릭터/컬렉션 게임화(경쟁사 약점으로 이미 명시된 방향과
  충돌), 광고 제거(워터링은 애초에 광고 없음), 위젯 자체 유료화(1탭 기록은 무료 유지, 테마만 유료화)

---

## 핵심 모듈 설명

### RecordSheet 플로우
```
OutlinedButton 클릭 (HomeScreen)
  → showRecordSheet = true
  → RecordSheet(ModalBottomSheet) 표시
    → RecordViewModel이 DrinkType / 용량 상태 관리
    → "기록하기" 버튼 → onRecord(amount, drinkType) 콜백
      → HomeViewModel.addWaterCustom(amount, drinkType)
        → WaterService.addWater() → DataStore 저장 → 위젯 갱신
      → onDismiss() → showRecordSheet = false
```

### DrinkType computed properties
`emoji`와 `displayName`은 `get()` 형태의 computed property로, `@Serializable`과 충돌 없이 안전하게 추가됨.

### Google Play Billing 구독 (BillingService + PremiumScreen)

- **BillingService** (`@Singleton`, `PurchasesUpdatedListener` 구현):
  - `startConnection()`: 앱 시작 시 `BillingClient` 연결 → 성공하면 `loadProducts()` + `queryPurchases()` 호출
  - `loadProducts()`: 구독 2종(SUBS) + 평생 상품 1종(INAPP) `queryProductDetails`
  - `launchPurchase()`: 구독은 `subscriptionOfferDetails`의 `offerToken` 필요, 평생 상품은 불필요 — 상품 타입별 분기
  - `queryPurchases()`: `queryPurchasesAsync`로 SUBS/INAPP 각각 조회 → 활성 구매 있으면 `isPremium=true` + `SettingsRepository.updatePremium()` 동기화 → 위젯/연속 기록 보호 로직에서 참조
  - `onPurchasesUpdated`: 구매 콜백에서 `acknowledgePurchase` 처리 후 `queryPurchases()` 재조회로 상태 갱신
  - 복원 버튼은 별도 API 없이 `queryPurchases()` 재호출로 충분 (Play는 계정에 귀속되어 자동 동기화)
- **PremiumScreen**: 기능 5종 카드 + 플랜 선택 카드 3종(월/연/평생) + 구매/복원 버튼, 이미 구독 중이면 안내 화면으로 분기
- **연속 기록 보호**: `WaterRepository.updateStreak(record, current, isPremium)` — `lastAchievedDateKey`가 정확히 2일 전이고(하루만 놓침) 프리미엄이며 이번 달 보호를 아직 안 썼을 때만 스트릭을 끊지 않고 유지. 2일 이상 공백은 보호 대상 아님(기존 로직대로 1로 리셋)

### 백업 및 복원 (AuthService + BackupService + BackupViewModel)

```
SettingsScreen "구글로 로그인" 클릭
  → BackupViewModel.signIn(activity)
    → AuthService.signIn(): Credential Manager로 GoogleIdOption 요청
      → 성공 시 FirebaseAuth.signInWithCredential() → currentUser 갱신
    → 성공 시 refreshLastBackupTimestamp() 자동 호출

"지금 백업" 클릭 → BackupViewModel.backup()
  → BackupService.backup(uid)
    → WaterRepository/SettingsRepository에서 todayRecord/streakInfo/history/settings 스냅샷 수집
    → JSON 문자열로 인코딩(encodeDefaults=true) 후 Firestore backups/{uid} 문서에 set() (항상 덮어쓰기)

"복원" 클릭 → BackupViewModel.restore()
  → BackupService.restore(uid)
    → backups/{uid} 문서 조회 → JSON 디코딩 → WaterRepository.restoreAll() + SettingsRepository.updateSettings()로 로컬 데이터 전체 교체
```

- 실시간 동기화가 아닌 수동 스냅샷 방식 — 여러 기기를 쓰는 경우 최신 기기에서 백업해야 한다는 안내가 UI에 노출됨
- `AuthService`는 백업/복원 전용 선택적 로그인. 로그인하지 않아도 앱 전체 기능은 그대로 동작 (온보딩 로그인과 무관)
- `BackupViewModel.init{}`이 로그인 상태면 `refreshLastBackupTimestamp()`를 즉시 호출 — `viewModelScope.launch{}`이므로 테스트에서는 `UnconfinedTestDispatcher` 때문에 ViewModel 생성 시점에 동기 실행됨에 유의 (테스트 작성 시 mock 스텁을 생성 전에 설정해야 함)

---

## 개발 이력

| 날짜 | 내용 |
|---|---|
| 2026-06-29 | [chore] 프로젝트 초기 설정, Gradle 빌드 환경 구성 (compileSdk=36, AGP 8.7.3) |
| 2026-06-29 | [feat] 코어 인프라 구현 (DataStore, Repository, Service, DI 모듈) |
| 2026-06-29 | [feat] HomeScreen v0.1 — 진행률 링, 기록 버튼, 스트릭, 타임라인 |
| 2026-06-29 | [feat] RecordSheet 구현 — 음료 종류/용량 선택 바텀시트, HomeScreen 연결 |
| 2026-06-30 | [feat] SettingsScreen v0.3 — 목표 잔 수/컵 크기/알림 설정 화면, SettingsViewModel 신규 |
| 2026-06-30 | [feat] 자정 초기화 WorkManager + 앱 아이콘 v0.4 — MidnightResetWorker, HiltWorkerFactory 설정, adaptive icon |
| 2026-06-30 | [feat] v0.5 — 위젯 3종 디자인 리뉴얼 (아쿠아/다크 테마), WateringWidgetState에 SettingsRepository 연결, HomeViewModel goal sync 수정 |
| 2026-06-30 | [feat] v0.5 — 성취 타이틀 시스템 (Achievement enum, AchievementChecker, AchievementDialog 풀스크린 모달) |
| 2026-06-30 | [fix] v0.6 — 위젯 크기 축소: CircularWidget 2×2→1×1, NarrowWidget 4×1→2×1, RectangularWidget 4×2→2×2 |
| 2026-06-30 | [fix] v0.6 — SettingsViewModel에 widgetUpdater 추가 (설정 변경 시 위젯 즉시 갱신) |
| 2026-06-30 | [feat] v0.7 — 위젯 다크/라이트 모드 자동 대응 (Configuration.UI_MODE_NIGHT_MASK 기반 색상 분기) |
| 2026-06-30 | [feat] v0.8 — 스플래시 화면 (SplashScreen.kt, NavGraph splash destination 추가), 앱 아이콘 리디자인 (그라데이션 배경 + 아웃라인 물방울) |
| 2026-06-30 | [fix] 시스템 스플래시 제거 — installSplashScreen() 삭제, Activity 테마 통일, 위젯 탭 갱신 로직 개선 |
| 2026-06-30 | [refactor] v0.9 — 코드 최적화: Dispatcher 수정, Dead Code 제거, WidgetEntryPoint 정리, 아이콘 전 밀도 배포 |
| 2026-06-30 | [chore] app/build 빌드 결과물 git 추적 해제 (.gitignore 업데이트, git rm --cached) |
| 2026-07-01 | [fix] AndroidManifest — SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM 불필요 권한 제거 (WorkManager만 사용, Play 정책 대응) |
| 2026-07-01 | [feat] SettingsScreen — 앱 정보 섹션 + 개인정보처리방침 링크 추가 |
| 2026-07-01 | [fix] 앱 아이콘 v0.9.2 — Adaptive Icon 재구현: 투명 배경 + 비네트 처리 전경(watering_icon3_fg.png), 하늘색 배경 링 개선 |
| 2026-07-01 | [fix] v0.9.3 — 물 기록 race condition, 위젯 과다 갱신, 죽은 취소 버튼, DataStore 에러 처리 누락, 금지 용어 수정 |
| 2026-07-01 | [fix] v0.9.4 — 위젯 연속 탭 시 화면 고정 버그 수정 (Glance 세션 재사용 시 상태 미갱신), 에뮬레이터 실기기 테스트로 검증 |
| 2026-07-02 | [feat] v0.10 — Google Play Billing 구독 구현 (BillingService, PremiumScreen 재작성, SettingsScreen 프리미엄 섹션, 연속 기록 보호 로직) |
| 2026-07-02 | [feat] v0.10.1 — 월간 구독 7일 무료체험 (BillingService 오퍼 선택 로직, PremiumScreen 배지/문구) |
| 2026-07-02 | [test] 2×1/2×2 위젯 연속 탭 갱신 검증 완료 — v0.9.4 수정 사항 회귀 없음 확인 (에뮬레이터 5연타 테스트) |
| 2026-07-02 | [refactor] v0.10.2 — 구독/프리미엄 코드 리뷰: 부분 캐시 버그 수정, 네트워크 조회 병렬화, LocalDate.now() 중복 호출 제거, PremiumScreen 오퍼 조회 중복 통합 |
| 2026-07-02 | [fix] v0.10.3 — 위젯 물방울 아이콘이 작은 크기에서 깨지던 문제 수정(호 경로→베지어 경로), 2×1 위젯 프로그레스 바가 100%에도 짧게만 채워지던 버그 수정 및 막대 길이 확장, 알림 간격 4시간 옵션 추가 |
| 2026-07-02 | [feat] v0.11 — 알림 권한 요청 플로우 (Android 13+): SettingsScreen에 권한 배너 + 시스템 설정 딥링크, ON_RESUME 재확인 |
| 2026-07-02 | [feat] v0.15 — Firebase Crashlytics + Analytics 연동 (google-services.json, 디버그 빌드 자동 수집 비활성화), 에뮬레이터 실행 검증 완료 |
| 2026-07-02 | [fix] v0.15.1 — Firebase Analytics가 자동 병합한 AD_ID 권한 제거(tools:node="remove" + adid_collection_enabled=false), 기획서 개인정보처리방침 문구를 실제 데이터 수집 내용에 맞게 갱신 |
| 2026-07-02 | [test] v0.16 — mockk/kotlinx-coroutines-test 도입, WaterRepository/SettingsRepository/WaterService/AchievementChecker/BillingService 단위 테스트 43개 작성 (전원 통과) |
| 2026-07-02 | [test] v0.17 — turbine 도입, MainDispatcherRule 신규, HomeViewModel/SettingsViewModel/OnboardingViewModel/PremiumViewModel/RecordViewModel/StatsViewModel 단위 테스트 41개 작성 (전체 84개 전원 통과) |
| 2026-07-02 | [docs] Google Play 스토어 등록 자료 1차 준비 — 아이콘 512×512, 스크린샷 6장, 전체/짧은 설명문 (`docs/store/`) |
| 2026-07-02 | [feat] v0.12 — 인앱 리뷰 요청 플로우(STREAK_7 달성 시 1회) + 위젯 다크모드 즉시 반영 버그 수정(프로세스 생존/재시작 이중 처리) |
| 2026-07-02 | [feat] v0.13 — 의견 보내기 mailto 버튼 (문의+개선요청 통합), 개발자 이메일 wateringapp.help@gmail.com 확정 |
| 2026-07-02 | [fix] v0.14 — 미구현 프리미엄 기능 4개 광고 제거(정책 리스크 대응), allowBackup 활성화, 문서-코드 정합성 전수 점검 |
| 2026-07-02 | [feat] v0.18 — 백업/복원 기능 구현: Google 로그인(Credential Manager) + Firestore 수동 백업/복원, AuthService/BackupService/AuthModule/BackupViewModel 신규, SettingsScreen UI 추가, 단위 테스트 14개 추가(전체 99개 통과), 에뮬레이터 UI 검증 완료 |
| 2026-07-03 | [feat] v0.24 — Health Connect 인프라 구축(프리미엄 3순위 Phase 2/4): HealthConnectService 신규, 매니페스트 권한/rationale 인텐트 추가, healthConnect 버전 카탈로그 오류(1.0.1→1.1.0) 수정, AGP 8.7.3→8.13.0 + Gradle 8.9→8.13 업그레이드, 단위 테스트 5개 추가(전체 135개 중 133개 통과, 실패 2건은 무관한 기존 AchievementCheckerTest 날짜 하드코딩 버그) |
| 2026-07-03 | [fix] v0.24.1 — AchievementChecker가 LocalDate.now() 대신 DayRecord.dateKey를 쓰도록 수정 (자정 경계 오작동 가능성 있던 잠재 버그), 전체 단위 테스트 135개 전원 통과 |
| 2026-07-03 | [feat] v0.25 — 체중 기반 스마트 목표 추천(프리미엄 3순위 Phase 3/4): HealthConnectService.readLatestWeightKg(), StatsInsightService.recommendedGoalCups() 추가, SettingsScreen/ViewModel에 권한 요청+추천 다이얼로그 흐름 구현, rationale 인텐트 액션 오류 수정(VIEW_PERMISSION_USAGE로 교체), PremiumScreen에 기능 추가, 단위 테스트 9개 추가(전체 144개 통과), Pixel_8(Android 14) 에뮬레이터에서 전체 흐름 실측 검증 완료 |
| 2026-07-04 | [feat] v0.26 — 물 마시기 기록 Health Connect 자동 저장(프리미엄 3순위 Phase 4/4, 무료): WaterService.addWater()에 best-effort 동기화 추가, HYDRATION_PERMISSIONS를 write 전용으로 축소, SettingsScreen에 건강 연동 토글, HydrationRecord startTime<endTime 제약 위반 버그 수정, testOptions.isReturnDefaultValues 추가, 단위 테스트 10개 추가/수정(전체 156개 통과), 에뮬레이터 실측 검증으로 실제 Health Connect 저장 확인 — 프리미엄 3순위 4단계 전체 완료 |
| 2026-07-04 | [feat] v0.27 — 연속 기록 마일스톤 확장(60/100/365일, 프리미엄 재기획 4순위): Achievement enum에 isPremiumOnly 필드 + STREAK_60/100/365 3종 추가, AchievementChecker.check()에 isPremium 파라미터(기본값) 추가해 프리미엄 게이팅 로직 구현, HomeViewModel 두 호출부에 isPremium 전달, 한국어/영문 strings.xml 양쪽에 신규 문자열 반영, 단위 테스트 9개 추가(전체 165개 통과) — 프리미엄 재기획 4순위 완료 |
| 2026-07-04 | [feat] v0.28 — 기록 CSV 내보내기(프리미엄 재기획 5순위): CsvExportService 신규(WaterRepository.getHistory() 기반 CSV 생성 + UTF-8 BOM + FileProvider 공유), AndroidManifest.xml에 FileProvider 최초 등록, SettingsViewModel/SettingsScreen에 CsvExportRow(WeightGoalRow와 동일 프리미엄 잠금 패턴) 추가, PremiumScreen에 Data Export 기능 추가, 단위 테스트 5개 추가(전체 170개 통과), Pixel_8 에뮬레이터에서 프리미엄/비프리미엄 양쪽 흐름 및 실제 CSV 파일 내용(BOM/헤더/행 데이터) 실측 검증 완료 — 프리미엄 재기획 5개 항목 전체 완료 |
| 2026-07-04 | [fix] v0.28.1 — 잠금화면 위젯 미노출 조사: Galaxy A25(Android 16) 실기기 테스트 중 발견, adb logcat으로 One UI Glance 템플릿 미리보기 생성 실패(`initialLayoutId : 0`) 원인 특정해 위젯 3종 xml에 `android:initialLayout="@layout/glance_default_loading_layout"` 추가(정당한 버그 수정으로 유지) — 재검증 결과 잠금화면 목록 자체 미노출은 One UI가 삼성 자사 앱만 화이트리스트로 노출하는 플랫폼 제약으로 결론(코드 해결 불가), 기획서 7-6절에 이미 명시된 리스크와 일치 |
| 2026-07-04 | [fix] v0.28.2 — 위젯 기록 시 성취 타이틀 미체크 갭 수정(v0.19 알려진 갭): `WidgetEntryPoint`에 `achievementChecker()` 추가, `AddWaterAction`이 기록 직전 상태를 캡처해 `achievementChecker.check(...)` 호출하도록 수정(dedup 갱신 + `achievement_unlocked` Analytics 이벤트 누락 해결), 전체 단위 테스트 통과 확인 — 위젯 기록 시 성취 모달을 앱 재진입 후 보여주는 것은 별도 후속 작업으로 남김 |
| 2026-07-04 | [fix] v0.28.3 — 실기기 실측 기반 위젯 UI 조정: 1×1 원형 위젯 "/n잔" 문구 제거 + 100% 미달 바 계산식 버그(패딩 변경 후 여백 상수 미반영) 수정 + 바 위치/길이 조정, 2×1 좁은 위젯 텍스트 14/12sp→18/14sp 확대 및 고정 텍스트 폭·여백 재조정("100%" 줄바꿈 회귀 발견 후 복원), 전체 실기기 실측으로 검증 |
| 2026-07-04 | [fix] v0.28.4 — 홈 화면 파스텔 팔레트 확정(Primary #5DCEE6, Secondary #F5C860 골드, Tertiary #FFC078) + 이모지→벡터 아이콘 전환: `HomeScreen.kt`의 하드코딩된 `AquaColor`/`GreenColor`를 제거하고 `MaterialTheme.colorScheme`를 직접 참조하도록 리팩터(테마 미반영 버그 수정), 🔥/🏆/💧(3곳) 이모지를 `Icons.Filled.LocalFireDepartment`/`EmojiEvents`/`WaterDrop` 벡터 아이콘으로 교체, 여러 파스텔 팔레트 후보를 HTML 목업 + 실기기 순차 적용으로 비교해 확정, 전체 단위 테스트 통과 확인 — 음료별(커피/차/주스/우유/기타) 아이콘은 스타일 미확정으로 보류 |
| 2026-07-04 | [fix] v0.28.5 — 미사용 위젯 딥링크(`watering://quick-record`) 제거: 정적 분석으로 `AddWaterAction` 백그라운드 기록 방식 도입 후 아무도 호출하지 않는 죽은 코드임을 확인, `AndroidManifest.xml`/`MainActivity.kt`/`WateringNavGraph.kt`/`HomeScreen.kt`에서 `quickRecord` 관련 코드 전체 제거, 전체 테스트+빌드 통과 확인. 문서-코드 정합성 서브에이전트 점검도 병행해 CLAUDE.md/기획서.md/개발보고서.md의 낡은 미구현 표기·버전·색상 hex 다수 정정 |
| 2026-07-04 | [docs] v0.28.6 — 개인정보처리방침 실제 게시본(Google Sites) 갱신: "외부 서버로 전송 없음" 문구가 Crashlytics/Analytics/백업(Firestore)/Health Connect 도입 후 더 이상 사실이 아님을 발견, 데이터 흐름을 명시하도록 사용자가 직접 게시본 수정 및 확인 완료 (코드 변경 없음) |
| 2026-07-04 | [fix] v0.29.0 — 기능 완성도 전수 점검 후 버그 5건 수정: 다크 모드 목표달성 색상이 그린으로 남아있던 것 골드로 통일, 기록 저장 실패 시 크래시 위험 제거(try/catch+스낵바 안내), `WaterRepository.updateStreak()`의 자정 경계 스트릭 버그 수정(시스템시각 대신 record.dateKey 기준, AchievementChecker와 동일 패턴), 위젯 로드 실패 시 CLAUDE.md 정책대로 마지막 성공 상태로 폴백하도록 실제 구현, 구독 확인(acknowledge) 실패 로깅 추가. 전체 테스트 통과 + 실기기 정상 실행 확인 |
| 2026-07-04 | [fix] v0.29.1 — 위젯 목표달성 색상 그린→골드 통일 + 다크모드 위젯 갱신 조사: 다크모드 전환 시 위젯이 안 바뀐다는 제보 조사 결과 감지 메커니즘 자체는 정상(앱 재시작 시 확실히 복구)이나 One UI 백그라운드 freeze로 인한 불안정 가능성 확인(사용자 결정으로 현행 유지), 조사 중 위젯의 `WidgetTheme.ACHIEVED_COLOR`가 앱과 달리 그린으로 남아있던 것 발견해 골드(#F5C860)로 통일, 실기기 재설치로 위젯 3종 반영 확인 |
| 2026-07-04 | [feat] v0.29.2 — 디버그 전용 프리미엄 테스트 토글 추가: `buildConfig=true` 활성화 후 `BuildConfig.DEBUG`로 감싼 "[DEBUG] 프리미엄 켜기/끄기" 버튼을 설정 화면에 추가(release 미노출), 기존 설정값(목표/컵크기 등) 훼손 없이 isPremium만 토글, 실기기에서 프리미엄 기능 전체(위젯테마/체중목표추천/CSV내보내기) 잠금 해제 확인 |
| 2026-07-04 | [feat] v0.29.3 — 프리미엄 테스트 제보 3건 조사: 위젯 테마 미반영/알림 미수신은 코드 버그가 아니라 목표 달성 상태에서의 의도된 동작으로 확인(조사만), 체중 데이터 없을 때 Health Connect 앱으로 바로 이동하는 버튼을 `NoWeightData` 다이얼로그에 추가(패키지 실행 인텐트, 미설치 시 플레이스토어 폴백), 전체 테스트 통과 |
| 2026-07-04 | [feat] v0.29.4 — 위젯 테마 파스텔 8종 확장 + 온보딩 다크모드 검정 글씨 버그 수정: 위젯 테마를 4종에서 사용자가 고른 파스텔 8종(아쿠아/민트/라벤더/코랄/로즈핑크/스카이블루/버터옐로우/그레이모노)으로 확장하고 Row를 FlowRow로 변경해 자연스럽게 줄바꿈되도록 수정, 온보딩 화면이 Scaffold/Surface 없이 시작해 다크모드에서 텍스트가 항상 검정으로 렌더링되던 버그를 Surface로 감싸서 수정, 실기기 확인 완료 |
| 2026-07-04 | [feat] v0.29.5 — 위젯 목표 달성 시 색상 반전: `ACHIEVED_COLOR`(고정 골드) 제거하고 3개 위젯 전부 달성 시 선택한 테마 색 배경 + 흰 텍스트로 반전되도록 수정(다크모드의 기존 미달성 스타일은 변화 없음), 테스트 통과. 실기기 최종 시각 확인은 터치 인식 이슈로 다음 세션으로 이월 |
| 2026-07-05 | [fix] v0.29.6 — 앱 아이콘 어댑티브 세이프존 크기 수정: `watering_icon3_fg.png`의 원형 배지+물방울 콘텐츠가 캔버스의 99.6%를 차지해 세이프존(약 61%)을 벗어나 런처 마스크에 잘리던 문제를 0.62배 축소+중앙 재배치로 수정, 실기기(Galaxy A25) 설치로 확인 |
| 2026-07-05 | [feat] v0.29.7 — 위젯 기록 시 업적 모달 미노출 갭 해소: `AchievementDataStore`에 pending_display 키 추가, `AddWaterAction`이 위젯에서 달성한 업적을 대기 저장하고 `HomeViewModel.init`이 앱 재진입 시 로드해 모달을 띄우도록 구현, `HomeViewModelTest` 2건 추가(전체 11개 통과). Health Connect 동기화 미노출 제보 조사 결과 코드 버그 아님 확인(워터링 쓰기 동기화는 정상, 삼성 헬스가 Health Connect 읽기 권한 미허용 상태인 것이 원인) |
| 2026-07-05 | [feat] v0.29.8 — Health Connect 다른 앱 미노출 안내 배너 추가: SettingsScreen "물 마시기 자동 저장" 토글 ON 시 삼성 헬스 등에서 안 보이는 이유와 Health Connect 설정 바로가기를 안내하는 배너 추가, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.9 — 체중 기반 목표 추천 프리미엄 제외(무료 전환) + 몸무게 직접 입력 방식으로 변경: `UserSettings.weightKg` 필드 추가, Health Connect 체중 읽기 관련 코드 전체 제거, `WeightGoalUiState` 단순화(7종→2종), 설정 화면에 kg 입력 다이얼로그 신규 구현(실시간 추천 잔수 계산), PremiumScreen 기능 목록에서 제거, 테스트 재작성(전체 170개 통과), 실기기 검증 완료. 홈 화면 배경에 스카이→라벤더 연한 그라데이션 적용(웹 목업 여러 차례 비교 후 확정), 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.10 — 연속 기록 카드/기록 리스트 배경색을 회색에서 아쿠아 틴트(#EAF7FB)로 변경: `HomeScreen.kt`에 `CardBackgroundColor` 상수 추가, 웹 목업 6종 비교 후 확정, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.11 — 목표 달성 색상을 골드에서 라벤더(소프트 퍼플 #BFA8DE)로 변경: `HomeScreen.kt`에 `AchievedColor` 상수 추가, 달성 링/"목표 달성!" 텍스트에만 적용(최장 연속 트로피는 골드 유지), 웹 목업 6종 비교 후 확정, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.12 — 홈 화면 "목표 달성!" 문구/이모지 크기 확대: `AchievementRing` 달성 문구를 `labelMedium`(12sp) 대신 `fontSize = 20.sp`로 직접 지정, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.13 — 2×2 위젯(Rectangular) 텍스트 크기 확대: 물방울 아이콘 13→16dp, "워터링" 라벨 11→13sp, 하단 동기부여 문구("목표 달성!" 포함) 11→13sp, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.14 — "다른 음료 선택" 버튼을 아쿠아 아웃라인에서 화이트 카드(테두리 없음+그림자+아쿠아 텍스트) 스타일로 변경, 웹 목업 6종 비교 후 확정, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.29.15 — "물 마셨어요" 메인 버튼에 은은한 그림자(elevation 2dp) 추가, 웹 목업 3단계 비교 후 확정, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.30.0 — 프리미엄 구독 제거, 전 기능 무료 전환: `UserSettings.isPremium` 필드 삭제, 위젯 테마/CSV 내보내기/스마트 통계/연속 기록 보호권/60·100·365일 마일스톤 전부 항상 무료로 동작하도록 변경, Settings/Stats/Home의 프리미엄 진입점(배너·자물쇠·네비게이션) 전부 제거(BillingService/PremiumScreen 코드 자체는 휴면 상태로 보존), 관련 테스트 전면 정리, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.30.1 — 연속 기록 카드/기록 리스트 배경색을 아쿠아 틴트에서 연한 크림(#FDFAF4)으로 변경, 웹 목업 여러 차례 비교 후 확정, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.30.2 — 설정 화면 "기록 설정" 섹션을 최상단으로 재배치, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.30.3 — Health Connect/백업 안내 배너를 버튼 없는 카드 전체 탭 가능형(화살표)으로 변경, 통계·설정 화면에 홈과 동일한 그라데이션 배경 적용(`AppBackgroundGradient` 공통화), 통계 화면 연속 기록 아이콘을 이모지에서 홈과 동일한 벡터 아이콘으로 통일, 실기기 확인 완료 |
| 2026-07-05 | [feat] v0.30.4 — 체중 기반 목표 추천 적용 후 스낵바 확인 메시지 추가, 설정 행 부제가 마지막 입력 이력을 반영하도록 변경, 관련 테스트 추가, 실기기 확인 완료 |
